export const snippets = [
  "DROP TABLE IF EXISTS orders;\nDROP TABLE IF EXISTS employees;\nCREATE TABLE employees (\n  id INTEGER PRIMARY KEY,\n  name TEXT NOT NULL,\n  department TEXT NOT NULL,\n  salary REAL NOT NULL,\n  hired_at TEXT NOT NULL\n);\nCREATE TABLE orders (\n  id INTEGER PRIMARY KEY,\n  customer_id INTEGER NOT NULL,\n  total REAL NOT NULL,\n  status TEXT NOT NULL,\n  ordered_at TEXT NOT NULL\n);\nINSERT INTO employees (id, name, department, salary, hired_at) VALUES\n  (1, 'Alice Chen', 'engineering', 95000.0, '2019-03-14'),\n  (2, 'Bob Martinez', 'engineering', 88000.0, '2020-07-01'),\n  (3, 'Carol Singh', 'sales', 72000.0, '2018-11-20'),\n  (4, 'David Kim', 'sales', 69000.0, '2021-01-05'),\n  (5, 'Elena Petrov', 'marketing', 78000.0, '2020-02-17'),\n  (6, 'Frank Osei', 'engineering', 102000.0, '2017-06-30');\nINSERT INTO orders (id, customer_id, total, status, ordered_at) VALUES\n  (1, 101, 250.0, 'shipped', '2024-01-15'),\n  (2, 102, 89.99, 'pending', '2024-02-03'),\n  (3, 101, 430.5, 'shipped', '2024-02-10'),\n  (4, 103, 120.0, 'cancelled', '2024-03-08'),\n  (5, 104, 310.75, 'shipped', '2024-03-22'),\n  (6, 102, 65.25, 'pending', '2024-04-12'),\n  (7, 105, 199.0, 'shipped', '2024-04-28');",
  "WITH RECURSIVE counter(n) AS (\n  SELECT 1 AS n\n  UNION ALL\n  SELECT n + 1 FROM counter\n  WHERE n < 30\n),\nlabeled AS (\n  SELECT\n    n,\n    CASE\n      WHEN n % 15 = 0 THEN 'fizzbuzz'\n      WHEN n % 3 = 0 THEN 'fizz'\n      WHEN n % 5 = 0 THEN 'buzz'\n      ELSE CAST(n AS TEXT)\n    END AS label\n  FROM counter\n),\nenriched AS (\n  SELECT\n    n,\n    label,\n    length(label) AS width,\n    CASE WHEN n % 2 = 0 THEN 'even' ELSE 'odd' END AS parity\n  FROM labeled\n)\nSELECT\n  e.n AS number,\n  e.label,\n  e.width,\n  e.parity,\n  round(100.0 * e.n / 30, 1) AS pct_of_range\nFROM enriched e\nORDER BY e.n;",
  "WITH ranked AS (\n  SELECT\n    id,\n    name,\n    department,\n    salary,\n    ROW_NUMBER() OVER (\n      PARTITION BY department\n      ORDER BY salary DESC\n    ) AS rank_in_dept,\n    SUM(salary) OVER (\n      PARTITION BY department\n      ORDER BY hired_at, id\n      ROWS BETWEEN UNBOUNDED PRECEDING\n        AND CURRENT ROW\n    ) AS running_payroll\n  FROM employees\n)\nSELECT\n  r.department,\n  r.name,\n  r.salary,\n  r.rank_in_dept,\n  round(r.running_payroll, 2) AS running_total,\n  round(\n    100.0 * r.salary / SUM(r.salary) OVER (PARTITION BY r.department),\n    1\n  ) AS pct_of_dept_payroll\nFROM ranked r\nORDER BY\n  r.department ASC,\n  r.rank_in_dept ASC;",
  "WITH monthly AS (\n  SELECT\n    strftime('%Y-%m', ordered_at) AS month_key,\n    COUNT(*) AS order_count,\n    SUM(total) AS revenue,\n    MIN(total) AS smallest_order,\n    MAX(total) AS largest_order,\n    AVG(total) AS avg_order_value,\n    COUNT(DISTINCT customer_id) AS active_customers\n  FROM orders\n  WHERE status <> 'cancelled'\n  GROUP BY strftime('%Y-%m', ordered_at)\n  HAVING COUNT(*) >= 2\n    AND SUM(total) > 0\n)\nSELECT\n  m.month_key,\n  m.order_count,\n  round(m.revenue, 2) AS revenue,\n  round(m.smallest_order, 2) AS smallest_order,\n  round(m.largest_order, 2) AS largest_order,\n  round(m.avg_order_value, 2) AS avg_order_value,\n  m.active_customers,\n  round(m.revenue / m.active_customers, 2) AS rev_per_customer,\n  CASE\n    WHEN m.avg_order_value > 200 THEN 'premium'\n    WHEN m.avg_order_value > 100 THEN 'standard'\n    ELSE 'budget'\n  END AS tier\nFROM monthly m\nORDER BY\n  m.month_key ASC;",
  "WITH ranked_salaries AS (\n  SELECT name, department, salary,\n    DENSE_RANK() OVER (\n      PARTITION BY department\n      ORDER BY salary DESC\n    ) AS salary_rank\n  FROM employees\n),\ntop_earners AS (\n  SELECT department, name, salary\n  FROM ranked_salaries\n  WHERE salary_rank = 1\n)\nSELECT\n  t.department,\n  t.name AS highest_paid,\n  t.salary AS top_salary,\n  (\n    SELECT COUNT(*) FROM employees e2\n    WHERE e2.department = t.department\n      AND e2.salary > t.salary * 0.8\n  ) AS near_top_peers,\n  (\n    SELECT AVG(e3.salary) FROM employees e3\n    WHERE e3.department = t.department\n  ) AS dept_avg_salary\nFROM top_earners t\nORDER BY t.salary DESC, t.department;",
];
