export const snippets = [
  "import std/[strutils, tables]\n\nproc topWords(text: string): CountTable[string] =\n  for raw in text.split():\n    let word = raw.toLowerAscii().strip(\n      leading = true, trailing = true,\n      chars = Whitespace + Punctuation)\n    if word.len > 1:\n      result.inc(word)\n\nlet passage = \"\"\"The quick brown fox jumps over the lazy dog.\nThe dog sleeps while the fox watches the hen house.\nQuick animals move fast but the lazy dog stays slow.\"\"\"\n\nlet counts = topWords(passage)\necho \"Word frequency table:\"\nfor word, count in pairs(counts):\n  echo \"  \", word, \": \", count\necho \"Distinct words kept: \", counts.len\n\nvar bestWord = \"\"\nvar bestCount = 0\nfor word, count in pairs(counts):\n  if count > bestCount:\n    bestCount = count\n    bestWord = word\necho \"Most common: \", bestWord, \" (\", bestCount, \" times)\"\n\nvar lonely = 0\nfor word, count in pairs(counts):\n  if count == 1:\n    inc lonely\necho \"Words seen only once: \", lonely",
  "import std/strutils\n\nproc isValidUsername(name: string): bool =\n  if name.len < 3 or name.len > 16:\n    return false\n  for c in name:\n    if not (c.isAlphaAscii() or c.isDigit() or c == '_'):\n      return false\n  result = name[0].isAlphaAscii()\n\nproc isStrongPassword(password: string): bool =\n  if password.len < 8:\n    return false\n  var hasUpper = false\n  var hasLower = false\n  var hasDigit = false\n  for c in password:\n    if c.isUpperAscii(): hasUpper = true\n    elif c.isLowerAscii(): hasLower = true\n    elif c.isDigit(): hasDigit = true\n  result = hasUpper and hasLower and hasDigit\n\nlet users = [\"amy\", \"Super_User9\", \"x\", \"bad-name!\", \"Coder_2024\"]\nlet passes = [\"short\", \"NoDigitsHere\", \"GoodPass123\", \"12345678\"]\n\nfor u in users:\n  echo \"Username '\", u, \"' valid: \", isValidUsername(u)\nfor p in passes:\n  echo \"Password strength ok: \", isStrongPassword(p)",
  "import std/[strutils, sequtils]\n\nproc describe(numbers: seq[float]): string =\n  var smallest = numbers[0]\n  var biggest = numbers[0]\n  var total = 0.0\n  for n in numbers:\n    if n < smallest:\n      smallest = n\n    if n > biggest:\n      biggest = n\n    total += n\n  let mean = total / numbers.len.float\n\n  let ordered = sorted(numbers)\n  let middle = ordered.len div 2\n  let median =\n    if ordered.len mod 2 == 1: ordered[middle]\n    else: (ordered[middle - 1] + ordered[middle]) / 2.0\n\n  result = \"count=\" & $numbers.len &\n           \" min=\" & $smallest & \" max=\" & $biggest &\n           \" mean=\" & mean.formatFloat(ffDecimal, 2) &\n           \" median=\" & median.formatFloat(ffDecimal, 2)\n\nlet readings = @[4.5, 2.1, 8.7, 3.3, 9.9, 6.6, 1.8]\necho describe(readings)\necho \"Second sample:\"\nlet tiny = @[10.0, 20.0, 30.0, 40.0]\necho describe(tiny)",
  "proc isOpening(c: char): bool =\n  result = c == '(' or c == '[' or c == '{'\n\nproc matches(left: char, right: char): bool =\n  result = (left == '(' and right == ')') or\n           (left == '[' and right == ']') or\n           (left == '{' and right == '}')\n\nproc isBalanced(text: string): bool =\n  var stack: seq[char] = @[]\n  for c in items(text):\n    if isOpening(c):\n      stack.add(c)\n    elif c == ')' or c == ']' or c == '}':\n      if stack.len == 0:\n        return false\n      let last = stack.pop()\n      if not matches(last, c):\n        return false\n  result = stack.len == 0\n\nlet tests = [\n  \"(a + b) * [c - d]\",\n  \"((nested) properly)\",\n  \"oops ) unbalanced\",\n  \"mixed ([)] wrong\"\n]\nfor t in tests:\n  if isBalanced(t):\n    echo \"Balanced: \", t\n  else:\n    echo \"Unbalanced: \", t",
];
