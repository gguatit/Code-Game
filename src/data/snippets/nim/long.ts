export const snippets = [
  "proc fibonacci(n: int): int =\n  var a = 0\n  var b = 1\n  if n <= 1:\n    return n\n  for i in 2 .. n:\n    let next = a + b\n    a = b\n    b = next\n  result = b\n\necho \"Fibonacci sequence generator\"\nvar numbers: seq[int] = @[]\nfor i in 0 ..< 15:\n  numbers.add(fibonacci(i))\n\nvar display = \"\"\nfor index, num in numbers:\n  display.add($num)\n  if index < numbers.len - 1:\n    display.add(\", \")\necho \"First 15: \", display\n\nvar total = 0\nfor num in numbers:\n  total += num\necho \"Total of sequence: \", total\necho \"Largest value: \", numbers[^1]",
  "proc partition(data: var seq[int], low: int, high: int): int =\n  let pivot = data[high]\n  var i = low - 1\n  for j in low ..< high:\n    if data[j] <= pivot:\n      inc i\n      swap(data[i], data[j])\n  swap(data[i + 1], data[high])\n  result = i + 1\n\nproc quickSort(data: var seq[int], low: int, high: int) =\n  if low < high:\n    let pivotIndex = partition(data, low, high)\n    quickSort(data, low, pivotIndex - 1)\n    quickSort(data, pivotIndex + 1, high)\n\nvar values = @[64, 25, 12, 22, 11, 90, 5, 45, 33, 78]\necho \"Original: \", values\nquickSort(values, 0, values.len - 1)\n\necho \"Sorted: \", values\nvar check = true\nfor i in 1 ..< values.len:\n  if values[i - 1] > values[i]:\n    check = false\nif check:\n  echo \"Array is correctly sorted\"\nelse:\n  echo \"Sorting failed somewhere\"",
  "proc binarySearch(values: seq[int], target: int): tuple[index: int, steps: int] =\n  var low = 0\n  var high = values.len - 1\n  var steps = 0\n  while low <= high:\n    let mid = low + (high - low) div 2\n    inc steps\n    if values[mid] == target:\n      return (mid, steps)\n    elif values[mid] < target:\n      low = mid + 1\n    else:\n      high = mid - 1\n  return (-1, steps)\n\nlet data = @[3, 8, 14, 21, 29, 36, 44, 52, 61, 70]\necho \"Sorted data: \", data\necho \"Hunting for several targets\"\n\nfor target in [14, 52, 3, 99]:\n  let found = binarySearch(data, target)\n  if found.index >= 0:\n    echo \"Found \", target, \" at index \", found.index\n  else:\n    echo \"Target \", target, \" not present\"\n  echo \"Steps taken: \", found.steps\n\necho \"Each step cuts the search range in half\"",
  "proc sieve(limit: int): seq[bool] =\n  var flags = newSeq[bool](limit + 1)\n  for i in 2 .. limit:\n    flags[i] = true\n  var current = 2\n  while current * current <= limit:\n    if flags[current]:\n      var multiple = current * current\n      while multiple <= limit:\n        flags[multiple] = false\n        multiple += current\n    inc current\n  result = flags\n\nlet limit = 60\nlet flags = sieve(limit)\nvar found: seq[int] = @[]\nfor n in 2 .. limit:\n  if flags[n]:\n    found.add(n)\n\necho \"Sieve of Eratosthenes up to \", limit\necho \"Primes: \", found\necho \"Count: \", found.len\n\nvar total = 0\nfor p in found:\n  total += p\necho \"Sum of all primes found: \", total\necho \"Largest prime below limit: \", found[^1]",
  "import std/strutils\n\nproc isAlphanumeric(c: char): bool =\n  result = (c >= 'a' and c <= 'z') or\n           (c >= 'A' and c <= 'Z') or\n           (c >= '0' and c <= '9')\n\nproc cleanUp(text: string): string =\n  for c in text.toLowerAscii():\n    if isAlphanumeric(c):\n      result.add(c)\n\nproc mirror(text: string): string =\n  for i in countdown(text.high, 0):\n    result.add(text[i])\n\nproc isPalindrome(text: string): bool =\n  let forward = cleanUp(text)\n  result = forward == mirror(forward)\n\necho \"Ignores spaces, punctuation and letter case\"\nlet samples = [\n  \"Racecar\",\n  \"Never odd or even\",\n  \"Hello, world!\",\n  \"Was it a car or a cat I saw?\"\n]\n\nfor sample in samples:\n  echo isPalindrome(sample), \" <- \", sample\n\necho \"Done checking all sample phrases\"",
];
