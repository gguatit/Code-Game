export const snippets = [
  "proc fib(n: int): int =\n  if n < 2:\n    return n\n  var (a, b) = (0, 1)\n  for _ in 2 .. n:\n    let next = a + b\n    a = b\n    b = next\n  b\n\nfor i in 0 .. 9:\n  echo \"fib(\", i, \") = \", fib(i)",
  "import std/sequtils\n\nproc quicksort(items: seq[int]): seq[int] =\n  if items.len <= 1:\n    return items\n  let pivot = items[items.len div 2]\n  let less = items.filter(proc(x: int): bool = x < pivot)\n  let equal = items.filter(proc(x: int): bool = x == pivot)\n  let greater = items.filter(proc(x: int): bool = x > pivot)\n  quicksort(less) & equal & quicksort(greater)\n\necho quicksort(@[5, 2, 9, 1, 7, 3, 8, 2])",
  "proc binarySearch(sorted: seq[int], target: int): int =\n  var lo = 0\n  var hi = sorted.high\n  while lo <= hi:\n    let mid = (lo + hi) div 2\n    if sorted[mid] == target:\n      return mid\n    elif sorted[mid] < target:\n      lo = mid + 1\n    else:\n      hi = mid - 1\n  -1\n\necho binarySearch(@[1, 3, 5, 7, 9], 7)",
  "import std/[math, sequtils]\n\nproc isPrime(n: int): bool =\n  if n < 2:\n    return false\n  for i in 2 .. int(sqrt(n.float)):\n    if n mod i == 0:\n      return false\n  true\n\nlet primes = toSeq(2 .. 30).filter(isPrime)\necho primes",
  "import std/strutils\n\nproc isPalindrome(text: string): bool =\n  var clean = \"\"\n  for ch in toLower(text):\n    if ch.isAlphaNumeric():\n      clean.add(ch)\n  var i = 0\n  var j = clean.high\n  while i < j:\n    if clean[i] != clean[j]:\n      return false\n    inc i\n    dec j\n  true\n\necho isPalindrome(\"Never odd or even\")\necho isPalindrome(\"hello world\")",
];
