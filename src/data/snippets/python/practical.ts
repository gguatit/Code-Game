export const snippets = [
  "def word_frequency(text):\n    counts = {}\n    for word in text.lower().split():\n        counts[word] = counts.get(word, 0) + 1\n    return sorted(counts.items(), key=lambda item: -item[1])\n\n\ntext = 'the quick brown fox jumps over the lazy dog'\nfor word, n in word_frequency(text):\n    print(word, n)",
  "def validate_user(user):\n    errors = []\n    if '@' not in user['email']:\n        errors.append('invalid email')\n    if len(user['password']) < 8:\n        errors.append('password too short')\n    if user['age'] < 18:\n        errors.append('must be an adult')\n    return {'ok': not errors, 'errors': errors}\n\n\nprint(validate_user({'email': 'a@b.com', 'password': 'longenough', 'age': 21}))\nprint(validate_user({'email': 'nope', 'password': 'short', 'age': 15}))",
  "def describe(numbers):\n    ordered = sorted(numbers)\n    mid = len(ordered) // 2\n    median = ordered[mid] if len(ordered) % 2 else (ordered[mid - 1] + ordered[mid]) / 2\n    return {\n        'min': ordered[0],\n        'max': ordered[-1],\n        'mean': sum(ordered) / len(ordered),\n        'median': median,\n    }\n\n\nprint(describe([4, 8, 15, 16, 23, 42]))",
  "def is_balanced(source):\n    pairs = {')': '(', ']': '[', '}': '{'}\n    stack = []\n    for ch in source:\n        if ch in '([{':\n            stack.append(ch)\n        elif ch in pairs and (not stack or stack.pop() != pairs[ch]):\n            return False\n    return not stack\n\n\nprint(is_balanced('f(a[0], { b: 1 })'))\nprint(is_balanced('f(a[0], { b: 1)'))",
];
