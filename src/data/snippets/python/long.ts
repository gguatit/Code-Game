export const snippets = [
  "def fibonacci(n: int) -> list[int]:\n    \"\"\"Return the first n Fibonacci numbers.\"\"\"\n    if n <= 0:\n        return []\n    sequence: list[int] = [0, 1]\n    if n == 1:\n        return [0]\n    while len(sequence) < n:\n        nxt = sequence[-1] + sequence[-2]\n        sequence.append(nxt)\n    return sequence[:n]\n\n\ndef describe(sequence: list[int]) -> str:\n    \"\"\"Build a short summary string for the sequence.\"\"\"\n    total = sum(sequence)\n    return f\"{len(sequence)} values, sum {total}, last {sequence[-1]}\"\n\n\ndef main() -> None:\n    count = int(input(\"How many Fibonacci numbers? \"))\n    numbers = fibonacci(count)\n    for index, value in enumerate(numbers, start=1):\n        print(f\"{index:>3}: {value}\")\n    print(describe(numbers))\n\n\nif __name__ == \"__main__\":\n    main()",
  "from collections.abc import Sequence\n\n\ndef quicksort(values: Sequence[int]) -> list[int]:\n    \"\"\"Sort integers with recursive quicksort around a middle pivot.\"\"\"\n    if len(values) <= 1:\n        return list(values)\n    pivot = values[len(values) // 2]\n    smaller = [v for v in values if v < pivot]\n    equal = [v for v in values if v == pivot]\n    larger = [v for v in values if v > pivot]\n    return quicksort(smaller) + equal + quicksort(larger)\n\n\ndef is_sorted(values: list[int]) -> bool:\n    \"\"\"Verify every adjacent pair is in ascending order.\"\"\"\n    return all(a <= b for a, b in zip(values, values[1:]))\n\n\ndef main() -> None:\n    data = [42, 7, 19, 73, 3, 56, 91, 28, 64, 11, 88, 35]\n    result = quicksort(data)\n    print(\"original:\", \" \".join(map(str, data)))\n    print(\"sorted:  \", \" \".join(map(str, result)))\n    print(\"verified:\", is_sorted(result))\n\n\nif __name__ == \"__main__\":\n    main()",
  "def binary_search(sorted_items: list[int], target: int) -> int:\n    \"\"\"Return the index of target, or -1 when it is missing.\"\"\"\n    low = 0\n    high = len(sorted_items) - 1\n    while low <= high:\n        mid = (low + high) // 2\n        guess = sorted_items[mid]\n        if guess == target:\n            return mid\n        if guess < target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1\n\n\ndef main() -> None:\n    numbers = [2, 5, 8, 12, 16, 23, 38, 56, 72, 91]\n    targets = [23, 91, 1, 72, 50]\n    print(f\"Searching in: {numbers}\")\n    for target in targets:\n        index = binary_search(numbers, target)\n        if index >= 0:\n            print(f\"{target:>3} found at index {index}\")\n        else:\n            print(f\"{target:>3} was not found\")\n\n\nif __name__ == \"__main__\":\n    main()",
  "def sieve(limit: int) -> list[bool]:\n    \"\"\"Mark every number up to limit as prime or composite.\"\"\"\n    flags = [True] * (limit + 1)\n    flags[0] = False\n    if limit >= 1:\n        flags[1] = False\n    current = 2\n    while current * current <= limit:\n        if flags[current]:\n            for multiple in range(current * current, limit + 1, current):\n                flags[multiple] = False\n        current += 1\n    return flags\n\n\ndef primes_up_to(limit: int) -> list[int]:\n    \"\"\"Collect the indices still marked prime.\"\"\"\n    return [number for number, flag in enumerate(sieve(limit)) if flag]\n\n\ndef main() -> None:\n    limit = int(input(\"Find all primes up to: \"))\n    primes = primes_up_to(limit)\n    print(f\"There are {len(primes)} primes up to {limit}.\")\n    print(\", \".join(str(p) for p in primes))\n\n\nif __name__ == \"__main__\":\n    main()",
  "import string\n\n\ndef clean_text(text: str) -> str:\n    \"\"\"Keep only ASCII letters and digits, lowercased.\"\"\"\n    allowed = set(string.ascii_lowercase + string.digits)\n    lowered = text.lower()\n    return \"\".join(ch for ch in lowered if ch in allowed)\n\n\ndef is_palindrome(text: str) -> bool:\n    \"\"\"Compare the cleaned text against its reverse.\"\"\"\n    cleaned = clean_text(text)\n    return cleaned == cleaned[::-1]\n\n\ndef main() -> None:\n    samples = [\n        \"A man, a plan, a canal: Panama\",\n        \"racecar\",\n        \"Hello, World!\",\n        \"12321\",\n        \"Was it a car or a cat I saw?\",\n    ]\n    for sample in samples:\n        verdict = \"yes\" if is_palindrome(sample) else \"no\"\n        print(f\"{sample!r:<36} -> palindrome: {verdict}\")\n\n\nif __name__ == \"__main__\":\n    main()",
];
