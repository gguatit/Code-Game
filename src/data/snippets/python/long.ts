export const snippets = [
  "def fib(n):\n    a, b = 0, 1\n    for _ in range(n):\n        a, b = b, a + b\n    return a\n\n\nfor n in (5, 10, 20):\n    print(f'fib({n}) = {fib(n)}')",
  "def quicksort(items):\n    if len(items) <= 1:\n        return items\n    pivot, *rest = items\n    left = [x for x in rest if x < pivot]\n    right = [x for x in rest if x >= pivot]\n    return quicksort(left) + [pivot] + quicksort(right)\n\n\ndata = [42, 7, 19, 3, 88, 23]\nprint(quicksort(data))\nprint(data)",
  "def binary_search(items, target):\n    lo, hi = 0, len(items) - 1\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if items[mid] == target:\n            return mid\n        if items[mid] < target:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n    return -1\n\n\nsorted_items = [1, 3, 5, 8, 13, 21, 34]\nprint(binary_search(sorted_items, 13))\nprint(binary_search(sorted_items, 4))",
  "def primes_up_to(limit):\n    sieve = [True] * (limit + 1)\n    sieve[0] = sieve[1] = False\n    result = []\n    for i in range(2, limit + 1):\n        if not sieve[i]:\n            continue\n        result.append(i)\n        for j in range(i * i, limit + 1, i):\n            sieve[j] = False\n    return result\n\n\nprint(primes_up_to(30))",
  "def is_palindrome(text):\n    clean = ''.join(ch for ch in text.lower() if ch.isalnum())\n    return clean == clean[::-1]\n\n\nprint(is_palindrome('Never odd or even'))\nprint(is_palindrome('Hello world'))",
];
