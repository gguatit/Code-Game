export const snippets = [
  "total = sum(x * x for x in nums)",
  "adults = [u for u in users if u.age >= 18]",
  "names = sorted(users, key=lambda u: u.name)",
  'print(", ".join(f"{k}={v}" for k, v in stats.items()))',
  "first, *rest = items",
  "value = data.get(\"key\", default=0)",
];
