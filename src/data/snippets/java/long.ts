export const snippets = [
  "import java.util.List;\nimport java.util.Map;\nimport java.util.stream.Collectors;\n\n/**\n * Aggregates orders into a per-customer revenue report.\n */\npublic class RevenueReport {\n    record Order(String customerId, long amountCents) {}\n\n    public static Map<String, Long> revenueByCustomer(List<Order> orders) {\n        return orders.stream().collect(\n            Collectors.groupingBy(Order::customerId,\n                Collectors.summingLong(Order::amountCents)));\n    }\n\n    public static void main(String[] args) {\n        var orders = List.of(new Order(\"c1\", 1999), new Order(\"c2\", 4500), new Order(\"c1\", 250));\n        revenueByCustomer(orders).forEach((customer, total) ->\n            System.out.printf(\"%s -> $%.2f%n\", customer, total / 100.0));\n    }\n}",
  "import java.util.concurrent.*;\n\n/**\n * Runs tasks with a bounded thread pool and collects results.\n */\npublic class ParallelRunner {\n    public static void main(String[] args) throws Exception {\n        ExecutorService pool = Executors.newFixedThreadPool(4);\n        try {\n            var futures = new java.util.ArrayList<Future<Integer>>();\n            for (int i = 0; i < 8; i++) {\n                final int task = i;\n                futures.add(pool.submit(() -> {\n                    Thread.sleep(100);\n                    return task * task;\n                }));\n            }\n            for (Future<Integer> future : futures) {\n                System.out.println(\"result = \" + future.get());\n            }\n        } finally {\n            pool.shutdown();\n            pool.awaitTermination(5, TimeUnit.SECONDS);\n        }\n    }\n}",
  "import java.util.Optional;\n\n/**\n * Small service layer showing Optional, records and exceptions together.\n */\npublic class UserService {\n    private final Map<Long, UserRecord> users = new java.util.concurrent.ConcurrentHashMap<>();\n    private long nextId = 1;\n\n    public record UserRecord(long id, String email, String displayName) {}\n\n    public UserRecord register(String email, String displayName) {\n        boolean taken = users.values().stream().anyMatch(u -> u.email().equalsIgnoreCase(email));\n        if (taken) throw new IllegalStateException(\"email already registered: \" + email);\n        UserRecord user = new UserRecord(nextId++, email, displayName);\n        users.put(user.id(), user);\n        return user;\n    }\n\n    public Optional<UserRecord> findByEmail(String email) {\n        return users.values().stream()\n            .filter(u -> u.email().equals(email))\n            .findFirst();\n    }\n}",
];
