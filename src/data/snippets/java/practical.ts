export const snippets = [
  "import java.util.HashMap;\nimport java.util.Map;\n\npublic class WordFrequency {\n    public static void main(String[] args) {\n        String sentence = \"the quick brown fox jumps over the lazy dog the fox runs\";\n        String[] words = sentence.toLowerCase().split(\" \");\n        Map<String, Integer> counts = new HashMap<>();\n        for (String word : words) {\n            counts.merge(word, 1, Integer::sum);\n        }\n        System.out.println(\"Word frequencies:\");\n        for (Map.Entry<String, Integer> entry : counts.entrySet()) {\n            System.out.println(entry.getKey() + \" => \" + entry.getValue());\n        }\n        String bestWord = \"\";\n        int bestCount = 0;\n        for (String key : counts.keySet()) {\n            int value = counts.get(key);\n            if (value > bestCount) {\n                bestWord = key;\n                bestCount = value;\n            }\n        }\n        System.out.println(\"Most frequent word: \" + bestWord);\n        System.out.println(\"Appeared \" + bestCount + \" times in the sentence\");\n        System.out.println(\"Unique words: \" + counts.size());\n        System.out.println(\"Total words scanned: \" + words.length);\n    }\n}",
  "public class UserValidator {\n    public static boolean isValidUsername(String username) {\n        if (username == null || username.length() < 4 || username.length() > 20) {\n            return false;\n        }\n        return username.matches(\"[a-zA-Z0-9_]+\") && Character.isLetter(username.charAt(0));\n    }\n    public static boolean isValidPassword(String password) {\n        if (password == null || password.length() < 8) {\n            return false;\n        }\n        boolean hasUpper = !password.equals(password.toLowerCase());\n        boolean hasLower = !password.equals(password.toUpperCase());\n        boolean hasDigit = password.matches(\".*[0-9].*\");\n        return hasUpper && hasLower && hasDigit;\n    }\n    public static void main(String[] args) {\n        String[] usernames = {\"alice_99\", \"1bad\", \"ok\", \"bob_the_dev\"};\n        String[] passwords = {\"SunnyDay42\", \"short\", \"alllowercase1\", \"NOLOWERCASE9\"};\n        for (String user : usernames) {\n            System.out.println(user + \" username valid: \" + isValidUsername(user));\n        }\n        for (String pass : passwords) {\n            System.out.println(pass + \" password valid: \" + isValidPassword(pass));\n        }\n        boolean canSignup = isValidUsername(\"alice_99\") && isValidPassword(\"SunnyDay42\");\n        System.out.println(\"Signup example passes both checks: \" + canSignup);\n    }\n}",
  "import java.util.Arrays;\n\npublic class StatsDescriber {\n    public static void describe(double[] values) {\n        double min = values[0];\n        double max = values[0];\n        double sum = 0;\n        for (double v : values) {\n            if (v < min) min = v;\n            if (v > max) max = v;\n            sum += v;\n        }\n        double mean = sum / values.length;\n        double[] sorted = values.clone();\n        Arrays.sort(sorted);\n        int mid = sorted.length / 2;\n        double median = (sorted.length % 2 == 0)\n            ? (sorted[mid - 1] + sorted[mid]) / 2.0\n            : sorted[mid];\n        System.out.println(\"count : \" + values.length);\n        System.out.println(\"min   : \" + min);\n        System.out.println(\"max   : \" + max);\n        System.out.println(\"mean  : \" + mean);\n        System.out.println(\"median: \" + median);\n    }\n    public static void main(String[] args) {\n        double[] sample = {8, 3, 15, 6, 21, 4, 12, 9, 17};\n        System.out.println(\"Describing sample of size \" + sample.length);\n        describe(sample);\n    }\n}",
  "import java.util.ArrayDeque;\nimport java.util.Deque;\npublic class BracketChecker {\n    public static boolean isBalanced(String input) {\n        Deque<Character> stack = new ArrayDeque<>();\n        for (char c : input.toCharArray()) {\n            if (c == '(' || c == '[' || c == '{') {\n                stack.push(c);\n            } else if (c == ')' || c == ']' || c == '}') {\n                if (stack.isEmpty()) {\n                    return false;\n                }\n                char open = stack.pop();\n                if (!matches(open, c)) {\n                    return false;\n                }\n            }\n        }\n        return stack.isEmpty();\n    }\n    private static boolean matches(char open, char close) {\n        return (open == '(' && close == ')')\n            || (open == '[' && close == ']')\n            || (open == '{' && close == '}');\n    }\n    public static void main(String[] args) {\n        String[] tests = {\"(a + b)\", \"{[()]}\", \"( [ ) ]\", \"\", \"no brackets here\"};\n        for (String test : tests) {\n            System.out.println(isBalanced(test) + \" <= \" + test);\n        }\n    }\n}",
];
