export const snippets = [
  "// Bank account service with transactions and audit logging.\nimport java.util.*;\nimport java.util.concurrent.locks.ReentrantLock;\n\npublic class AccountService {\n    public static final class InsufficientFundsException extends RuntimeException {\n        public InsufficientFundsException(long missing) {\n            super(\"missing \" + missing + \" cents\");\n        }\n    }\n\n    public static final class Account {\n        final String owner;\n        long balanceCents;\n        final ReentrantLock lock = new ReentrantLock();\n\n        Account(String owner, long initialCents) {\n            this.owner = owner;\n            this.balanceCents = initialCents;\n        }\n    }\n\n    private final Map<String, Account> accounts = new HashMap<>();\n    private final List<String> auditLog = new ArrayList<>();\n\n    public Account open(String id, String owner, long initialCents) {\n        Account account = new Account(owner, initialCents);\n        accounts.put(id, account);\n        audit(id, \"opened with \" + initialCents);\n        return account;\n    }\n\n    public void transfer(String fromId, String toId, long amountCents) {\n        if (amountCents <= 0) throw new IllegalArgumentException(\"amount must be positive\");\n        Account from = accounts.get(fromId);\n        Account to = accounts.get(toId);\n        Objects.requireNonNull(from, \"unknown source account\");\n        Objects.requireNonNull(to, \"unknown target account\");\n\n        // lock in deterministic order to avoid deadlock\n        Account first = fromId.compareTo(toId) < 0 ? from : to;\n        Account second = first == from ? to : from;\n        first.lock.lock();\n        second.lock.lock();\n        try {\n            if (from.balanceCents < amountCents) {\n                throw new InsufficientFundsException(amountCents - from.balanceCents);\n            }\n            from.balanceCents -= amountCents;\n            to.balanceCents += amountCents;\n            audit(fromId, \"-\" + amountCents + \" to \" + toId);\n            audit(toId, \"+\" + amountCents + \" from \" + fromId);\n        } finally {\n            first.lock.unlock();\n            second.lock.unlock();\n        }\n    }\n\n    public long balance(String accountId) {\n        Account account = accounts.get(accountId);\n        if (account == null) throw new NoSuchElementException(accountId);\n        return account.balanceCents;\n    }\n\n    private void audit(String accountId, String message) {\n        auditLog.add(System.currentTimeMillis() + \" [\" + accountId + \"] \" + message);\n    }\n\n    public List<String> auditLog() {\n        return List.copyOf(auditLog);\n    }\n\n    public static void main(String[] args) {\n        AccountService service = new AccountService();\n        service.open(\"a1\", \"alice\", 10_000);\n        service.open(\"a2\", \"bob\", 250);\n        service.transfer(\"a1\", \"a2\", 1_500);\n        System.out.println(\"alice=\" + service.balance(\"a1\"));\n        System.out.println(\"bob=\" + service.balance(\"a2\"));\n        service.auditLog().forEach(System.out::println);\n    }\n}",
  "// Task scheduler demo: priority queue plus worker simulation.\nimport java.util.*;\n\npublic class TaskScheduler {\n    record Task(int priority, String name, int costMs) implements Comparable<Task> {\n        @Override\n        public int compareTo(Task other) {\n            return Integer.compare(other.priority, this.priority); // high priority first\n        }\n    }\n\n    private final PriorityQueue<Task> queue = new PriorityQueue<>();\n\n    public synchronized void submit(Task task) {\n        queue.offer(task);\n        notifyAll();\n    }\n\n    public synchronized Task take() throws InterruptedException {\n        while (queue.isEmpty()) {\n            wait();\n        }\n        return queue.poll();\n    }\n\n    public static void main(String[] args) throws Exception {\n        TaskScheduler scheduler = new TaskScheduler();\n        scheduler.submit(new Task(1, \"cleanup-temp-files\", 120));\n        scheduler.submit(new Task(9, \"flush-payment-queue\", 300));\n        scheduler.submit(new Task(5, \"rebuild-search-index\", 900));\n\n        Thread worker = new Thread(() -> {\n            try {\n                while (!Thread.currentThread().isInterrupted()) {\n                    Task task = scheduler.take();\n                    System.out.println(\"running p\" + task.priority() + \" -> \" + task.name());\n                    Thread.sleep(task.costMs() / 10);\n                }\n            } catch (InterruptedException ignored) {\n                Thread.currentThread().interrupt();\n            }\n        });\n        worker.start();\n        Thread.sleep(500);\n        worker.interrupt();\n        worker.join();\n        System.out.println(\"scheduler stopped\");\n    }\n}",
];
