export const snippets = [
  "name = \"Ruby\"",
  "puts \"Hello, world!\"",
  "def add(a, b) = a + b",
  "@users.each { |user| puts user.name }",
  "list.map { |x| x * 2 }.select(&:even?)",
  "attr_accessor :name, :email",
  "raise ArgumentError, 'bad input' unless valid?",
  "%w[apple banana cherry]",
  "hash.fetch(:key, 'default')",
  "5.times { |i| print i }",
];
