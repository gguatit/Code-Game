export const snippets = [
  "def build_fibonacci(count)\n  numbers = [0, 1]\n  return [0] if count == 1\n  return [] if count <= 0\n\n  while numbers.length < count\n    numbers << numbers[-2] + numbers[-1]\n  end\n  numbers.take(count)\nend\n\ndef print_table(numbers)\n  total = numbers.sum\n  numbers.each_with_index do |value, index|\n    position = index + 1\n    bar = '*' * (value.to_s.length + 1)\n    puts format('%4d. %-12s (%s)', position, value.to_s, bar)\n  end\n  puts '-' * 22\n  puts \"Total of all terms: #{total}\"\nend\n\nprint 'Enter how many Fibonacci numbers you want: '\ncount = gets.to_i\n\nif count < 1\n  puts 'Please enter a positive integer.'\nelse\n  print_table(build_fibonacci(count))\nend",
  "def quicksort(items)\n  return items if items.length <= 1\n\n  pivot, *rest = items\n  smaller = rest.select { |item| item < pivot }\n  larger = rest.select { |item| item >= pivot }\n\n  quicksort(smaller) + [pivot] + quicksort(larger)\nend\n\ndef show_sort_demo(values)\n  puts \"Input:  #{values.inspect}\"\n  sorted = quicksort(values)\n  puts \"Sorted: #{sorted.inspect}\"\nend\n\nsamples = [\n  [5, 3, 8, 1, 9, 2],\n  [42],\n  [],\n  [7, 7, 3, 3, 1],\n  [100, -4, 55, 0, -4]\n]\n\nsamples.each do |sample|\n  show_sort_demo(sample)\nend\n\nputs 'All sample lists were sorted correctly.'\n\nlargest = quicksort(samples.first).last\nputs \"Largest value in the first sample: #{largest}\"",
  "def binary_search(sorted_items, target)\n  low = 0\n  high = sorted_items.length - 1\n\n  while low <= high\n    middle = (low + high) / 2\n    guess = sorted_items[middle]\n\n    if guess == target\n      return middle\n    elsif guess < target\n      low = middle + 1\n    else\n      high = middle - 1\n    end\n  end\n\n  nil\nend\n\nnumbers = (1..40).step(3).to_a\nputs \"Search list: #{numbers.inspect}\"\n\n[7, 22, 40, 5, 39].each do |target|\n  position = binary_search(numbers, target)\n  if position\n    puts \"Found #{target} at index #{position}.\"\n  else\n    puts \"#{target} is not present.\"\n  end\nend",
  "def sieve(limit)\n  return [] if limit < 2\n\n  flags = Array.new(limit + 1, true)\n  flags[0] = false\n  flags[1] = false\n\n  number = 2\n  while number * number <= limit\n    if flags[number]\n      multiple = number * number\n      while multiple <= limit\n        flags[multiple] = false\n        multiple += number\n      end\n    end\n    number += 1\n  end\n\n  flags.each_index.select { |index| flags[index] }\nend\n\nprimes = sieve(60)\nputs \"Primes up to 60: #{primes.inspect}\"\nputs \"Count: #{primes.length}\"\nlargest = primes.last\nputs \"Largest prime found: #{largest}\"\ngaps = primes.each_cons(2).map { |first, second| second - first }\nputs \"Gaps between primes: #{gaps.inspect}\"\n\ntwin_pairs = primes.each_cons(2).select { |first, second| second - first == 2 }\nputs \"Twin prime pairs: #{twin_pairs.inspect}\"",
  "def letters_and_digits_only(text)\n  text.downcase.chars.select do |char|\n    ('a'..'z').cover?(char) || ('0'..'9').cover?(char)\n  end.join\nend\n\ndef palindrome?(text)\n  cleaned = letters_and_digits_only(text)\n  reversed = cleaned.reverse\n  cleaned == reversed\nend\n\ncandidates = [\n  'A man, a plan, a canal: Panama',\n  'Was it a car or a cat I saw?',\n  'No lemon, no melon',\n  'Hello, world',\n  '12321',\n  'Step on no pets'\n]\n\ncandidates.each do |candidate|\n  verdict = palindrome?(candidate) ? 'yes' : 'no'\n  puts \"#{candidate} -> palindrome: #{verdict}\"\nend\n\npassed = candidates.count { |candidate| palindrome?(candidate) }\nputs \"#{passed} of #{candidates.length} phrases qualified\"\nputs 'Palindrome scan complete.'",
];
