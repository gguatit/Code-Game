export const snippets = [
  "def word_frequency(text)\n  counts = Hash.new(0)\n  text.downcase.split.each { |word| counts[word] += 1 }\n  counts.sort_by { |_, n| -n }.to_h\nend\n\ntext = 'the quick brown fox jumps over the lazy dog'\nword_frequency(text).each do |word, n|\n  puts \"#{word} #{n}\"\nend",
  "User = Struct.new(:email, :password, :age) do\n  def errors\n    list = []\n    list << 'invalid email' unless email.include?('@')\n    list << 'password too short' if password.length < 8\n    list << 'must be an adult' if age < 18\n    list\n  end\nend\n\nputs User.new('a@b.com', 'longenough', 21).errors.inspect\nputs User.new('nope', 'short', 15).errors.inspect",
  "def describe(numbers)\n  sorted = numbers.sort\n  {\n    min: sorted.first,\n    max: sorted.last,\n    mean: numbers.sum.to_f / numbers.size\n  }\nend\n\np describe([4, 8, 15, 16, 23, 42])",
  "def balanced?(source)\n  pairs = { ')' => '(', ']' => '[', '}' => '{' }\n  stack = []\n  source.each_char do |ch|\n    if '([{'.include?(ch)\n      stack.push(ch)\n    elsif pairs.key?(ch) && stack.pop != pairs[ch]\n      return false\n    end\n  end\n  stack.empty?\nend\n\np balanced?('f(a[0], { b: 1 })')\np balanced?('f(a[0], { b: 1)')",
];
