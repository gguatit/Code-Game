export const snippets = [
  "map(&:price).sum",
  "items.each_with_index { |item, i|",
  "user&.profile&.email",
  "%w[a b c].each do |x|",
  "def initialize(name:); @name = name; end",
  "arr.select { |n| n.even? }",
];
