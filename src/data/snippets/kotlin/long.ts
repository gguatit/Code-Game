export const snippets = [
  "fun fibonacci(count: Int): List<Long> {\n    val sequence = mutableListOf<Long>()\n    var previous = 0L\n    var current = 1L\n    repeat(count) {\n        sequence.add(previous)\n        val next = previous + current\n        previous = current\n        current = next\n    }\n    return sequence\n}\n\nfun main() {\n    print(\"How many Fibonacci numbers? \")\n    val count = readLine()?.trim()?.toIntOrNull() ?: 10\n    if (count <= 0) {\n        println(\"Please enter a positive number.\")\n        return\n    }\n    val numbers = fibonacci(count)\n    println(\"First $count Fibonacci numbers:\")\n    numbers.forEachIndexed { index, value ->\n        println(\"%02d: %d\".format(index + 1, value))\n    }\n    val largest = numbers.last()\n    println(\"Largest value generated: $largest\")\n}",
  "fun quicksort(items: MutableList<Int>, low: Int, high: Int) {\n    if (low >= high) return\n    val pivot = items[(low + high) / 2]\n    var i = low\n    var j = high\n    while (i <= j) {\n        while (items[i] < pivot) i++\n        while (items[j] > pivot) j--\n        if (i <= j) {\n            val temp = items[i]\n            items[i] = items[j]\n            items[j] = temp\n            i++\n            j--\n        }\n    }\n    quicksort(items, low, j)\n    quicksort(items, i, high)\n}\n\nfun main() {\n    val data = mutableListOf(42, 7, 19, 88, 3, 56, 23, 91, 14, 67)\n    println(\"Before sorting: $data\")\n    quicksort(data, 0, data.size - 1)\n    println(\"After sorting:  $data\")\n    val check = data.zipWithNext().all { (a, b) -> a <= b }\n    println(\"Sorted correctly: $check\")\n}",
  "fun binarySearch(sorted: List<Int>, target: Int): Int {\n    var low = 0\n    var high = sorted.size - 1\n    var steps = 0\n    while (low <= high) {\n        steps++\n        val mid = (low + high) / 2\n        when {\n            sorted[mid] == target -> {\n                println(\"Found after $steps step(s).\")\n                return mid\n            }\n            sorted[mid] < target -> low = mid + 1\n            else -> high = mid - 1\n        }\n    }\n    println(\"Not found after $steps step(s).\")\n    return -1\n}\n\nfun main() {\n    val numbers = listOf(2, 5, 8, 12, 16, 23, 38, 56, 72, 91)\n    println(\"Searching in: $numbers\")\n    for (target in listOf(23, 5, 91, 40)) {\n        val index = binarySearch(numbers, target)\n        println(\"Target $target resolved to index $index.\")\n    }\n    println(\"Binary search needs only log2(n) comparisons.\")\n}",
  "fun sieveOfEratosthenes(limit: Int): List<Int> {\n    if (limit < 2) return emptyList()\n    val isComposite = BooleanArray(limit + 1)\n    val primes = mutableListOf<Int>()\n    for (number in 2..limit) {\n        if (!isComposite[number]) {\n            primes.add(number)\n            var multiple = number * number\n            while (multiple <= limit) {\n                isComposite[multiple] = true\n                multiple += number\n            }\n        }\n    }\n    return primes\n}\n\nfun main() {\n    val limit = 100\n    val primes = sieveOfEratosthenes(limit)\n    println(\"Primes up to $limit (${primes.size} total):\")\n    primes.chunked(10).forEach { row ->\n        println(row.joinToString(\" \") { \"%3d\".format(it) })\n    }\n    val total = primes.sum()\n    println(\"Sum of all primes: $total\")\n    println(\"Largest prime found: ${primes.last()}\")\n}",
  "fun normalize(text: String): String {\n    return text.lowercase().filter { it.isLetterOrDigit() }\n}\n\nfun isPalindrome(text: String): Boolean {\n    val clean = normalize(text)\n    if (clean.isEmpty()) return true\n    var left = 0\n    var right = clean.length - 1\n    while (left < right) {\n        if (clean[left] != clean[right]) return false\n        left++\n        right--\n    }\n    return true\n}\n\nfun main() {\n    val samples = listOf(\n        \"A man, a plan, a canal: Panama\",\n        \"Was it a car or a cat I saw?\",\n        \"Hello, World\",\n        \"No lemon, no melon\"\n    )\n    for (sample in samples) {\n        val result = if (isPalindrome(sample)) \"is\" else \"is not\"\n        println(\"\\\"$sample\\\" $result a palindrome.\")\n    }\n}",
];
