export const snippets = [
  "fun fib(n: Int): Long {\n    var a = 0L\n    var b = 1L\n    repeat(n) {\n        val next = a + b\n        a = b\n        b = next\n    }\n    return a\n}\n\nfun main() {\n    for (n in listOf(5, 10, 20)) {\n        println(\"fib($n) = ${fib(n)}\")\n    }\n}",
  "fun quicksort(items: List<Int>): List<Int> {\n    if (items.size <= 1) return items\n    val pivot = items.first()\n    val rest = items.drop(1)\n    val left = rest.filter { it < pivot }\n    val right = rest.filter { it >= pivot }\n    return quicksort(left) + pivot + quicksort(right)\n}\n\nfun main() {\n    println(quicksort(listOf(42, 7, 19, 3, 88, 23)))\n}",
  "fun binarySearch(items: IntArray, target: Int): Int {\n    var lo = 0\n    var hi = items.size - 1\n    while (lo <= hi) {\n        val mid = (lo + hi) / 2\n        when {\n            items[mid] == target -> return mid\n            items[mid] < target -> lo = mid + 1\n            else -> hi = mid - 1\n        }\n    }\n    return -1\n}\n\nfun main() {\n    val sorted = intArrayOf(1, 3, 5, 8, 13, 21, 34)\n    println(binarySearch(sorted, 13))\n    println(binarySearch(sorted, 4))\n}",
  "fun primesUpTo(limit: Int): List<Int> {\n    val sieve = BooleanArray(limit + 1) { true }\n    sieve[0] = false\n    if (limit >= 1) sieve[1] = false\n    val result = mutableListOf<Int>()\n    for (i in 2..limit) {\n        if (!sieve[i]) continue\n        result.add(i)\n        var j = i * i\n        while (j <= limit) {\n            sieve[j] = false\n            j += i\n        }\n    }\n    return result\n}\n\nfun main() {\n    println(primesUpTo(30))\n}",
  "fun isPalindrome(text: String): Boolean {\n    val clean = text.lowercase().filter { it.isLetterOrDigit() }\n    var left = 0\n    var right = clean.length - 1\n    while (left < right) {\n        if (clean[left] != clean[right]) return false\n        left++\n        right--\n    }\n    return true\n}\n\nfun main() {\n    println(isPalindrome(\"Never odd or even\"))\n    println(isPalindrome(\"Hello world\"))\n}",
];
