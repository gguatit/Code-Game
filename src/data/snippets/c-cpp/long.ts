export const snippets = [
  "#include <stdio.h>\n\nlong long fib(int n) {\n    long long a = 0;\n    long long b = 1;\n    for (int i = 0; i < n; i++) {\n        long long next = a + b;\n        a = b;\n        b = next;\n    }\n    return a;\n}\n\nint main(void) {\n    int sizes[] = { 5, 10, 20 };\n    for (int i = 0; i < 3; i++) {\n        printf(\"fib(%d) = %lld\\n\", sizes[i], fib(sizes[i]));\n    }\n    return 0;\n}",
  "#include <stdio.h>\n\nvoid quicksort(int *items, int lo, int hi) {\n    if (lo >= hi) return;\n    int pivot = items[(lo + hi) / 2];\n    int i = lo;\n    int j = hi;\n    while (i <= j) {\n        while (items[i] < pivot) i++;\n        while (items[j] > pivot) j--;\n        if (i <= j) {\n            int tmp = items[i];\n            items[i] = items[j];\n            items[j] = tmp;\n            i++;\n            j--;\n        }\n    }\n    quicksort(items, lo, j);\n    quicksort(items, i, hi);\n}",
  "#include <stdio.h>\n\nint binary_search(const int *items, int size, int target) {\n    int lo = 0;\n    int hi = size - 1;\n    while (lo <= hi) {\n        int mid = (lo + hi) / 2;\n        if (items[mid] == target) return mid;\n        if (items[mid] < target) lo = mid + 1;\n        else hi = mid - 1;\n    }\n    return -1;\n}\n\nint main(void) {\n    int sorted[] = { 1, 3, 5, 8, 13, 21, 34 };\n    printf(\"%d\\n\", binary_search(sorted, 7, 13));\n    printf(\"%d\\n\", binary_search(sorted, 7, 4));\n    return 0;\n}",
  "#include <stdio.h>\n\n#define LIMIT 30\n\nint main(void) {\n    int sieve[LIMIT + 1] = { 0 };\n    for (int i = 2; i <= LIMIT; i++) sieve[i] = 1;\n    for (int i = 2; i * i <= LIMIT; i++) {\n        if (!sieve[i]) continue;\n        for (int j = i * i; j <= LIMIT; j += i) sieve[j] = 0;\n    }\n    for (int i = 2; i <= LIMIT; i++) {\n        if (sieve[i]) printf(\"%d \", i);\n    }\n    printf(\"\\n\");\n    return 0;\n}",
  "#include <stdio.h>\n#include <string.h>\n#include <ctype.h>\n\nint is_palindrome(const char *text) {\n    char clean[128];\n    int n = 0;\n    for (const char *p = text; *p; p++) {\n        if (isalnum((unsigned char)*p)) clean[n++] = tolower(*p);\n    }\n    clean[n] = '\\0';\n    for (int i = 0, j = n - 1; i < j; i++, j--) {\n        if (clean[i] != clean[j]) return 0;\n    }\n    return 1;\n}\n\nint main(void) {\n    printf(\"%d\\n\", is_palindrome(\"Never odd or even\"));\n    return 0;\n}",
];
