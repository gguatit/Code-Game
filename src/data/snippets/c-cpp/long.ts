export const snippets = [
  "#include <iostream>\n#include <vector>\n\nstd::vector<long long> fibonacci(int n) {\n    std::vector<long long> result;\n    long long a = 0;\n    long long b = 1;\n    for (int i = 0; i < n; ++i) {\n        result.push_back(a);\n        long long next = a + b;\n        a = b;\n        b = next;\n    }\n    return result;\n}\n\nint main() {\n    int count = 15;\n    std::vector<long long> numbers = fibonacci(count);\n    std::cout << \"First \" << count << \" Fibonacci numbers:\" << std::endl;\n    for (size_t i = 0; i < numbers.size(); ++i) {\n        std::cout << i + 1 << \": \" << numbers[i];\n        if (i + 1 < numbers.size()) {\n            std::cout << \", \";\n        }\n    }\n    std::cout << std::endl;\n    return 0;\n}",
  "#include <iostream>\n#include <vector>\n\nint partition(std::vector<int>& values, int low, int high) {\n    int pivot = values[high];\n    int i = low - 1;\n    for (int j = low; j < high; ++j) {\n        if (values[j] <= pivot) {\n            ++i;\n            std::swap(values[i], values[j]);\n        }\n    }\n    std::swap(values[i + 1], values[high]);\n    return i + 1;\n}\n\nvoid quicksort(std::vector<int>& values, int low, int high) {\n    if (low < high) {\n        int pivotIndex = partition(values, low, high);\n        quicksort(values, low, pivotIndex - 1);\n        quicksort(values, pivotIndex + 1, high);\n    }\n}\nint main() {\n    std::vector<int> data = {42, 7, 19, 3, 88, 25, 56, 1};\n    quicksort(data, 0, static_cast<int>(data.size()) - 1);\n    std::cout << \"Sorted:\";\n    for (int value : data) {\n        std::cout << \" \" << value;\n    }\n    std::cout << \"\\n\";\n    return 0;\n}",
  "#include <iostream>\n#include <vector>\n\nint binarySearch(const std::vector<int>& data, int target) {\n    int left = 0;\n    int right = static_cast<int>(data.size()) - 1;\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        if (data[mid] == target) {\n            return mid;\n        } else if (data[mid] < target) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    return -1;\n}\n\nint main() {\n    std::vector<int> sorted = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n    int targets[] = {23, 91, 4};\n    for (int t : targets) {\n        int index = binarySearch(sorted, t);\n        if (index >= 0) {\n            std::cout << t << \" found at index \" << index << \"\\n\";\n        } else {\n            std::cout << t << \" not found\\n\";\n        }\n    }\n    return 0;\n}",
  "#include <iostream>\n#include <vector>\n\nstd::vector<int> sievePrimes(int limit) {\n    std::vector<bool> isPrime(limit + 1, true);\n    isPrime[0] = false;\n    isPrime[1] = false;\n    for (int p = 2; p * p <= limit; ++p) {\n        if (isPrime[p]) {\n            for (int multiple = p * p; multiple <= limit; multiple += p) {\n                isPrime[multiple] = false;\n            }\n        }\n    }\n    std::vector<int> primes;\n    for (int n = 2; n <= limit; ++n) {\n        if (isPrime[n]) {\n            primes.push_back(n);\n        }\n    }\n    return primes;\n}\n\nint main() {\n    int limit = 60;\n    std::vector<int> primes = sievePrimes(limit);\n    std::cout << \"Primes up to \" << limit << \":\" << std::endl;\n    for (int p : primes) {\n        std::cout << p << \" \";\n    }\n    std::cout << \"\\nFound \" << primes.size() << \" primes.\" << std::endl;\n    return 0;\n}",
  "#include <cctype>\n#include <iostream>\n#include <string>\n\nbool isPalindrome(const std::string& text) {\n    int left = 0;\n    int right = static_cast<int>(text.size()) - 1;\n    while (left < right) {\n        while (left < right && !std::isalnum(text[left])) {\n            ++left;\n        }\n        while (left < right && !std::isalnum(text[right])) {\n            --right;\n        }\n        if (std::tolower(text[left]) != std::tolower(text[right])) {\n            return false;\n        }\n        ++left;\n        --right;\n    }\n    return true;\n}\n\nint main() {\n    std::string samples[] = {\"A man, a plan, a canal: Panama\", \"No lemon, no melon\", \"hello\"};\n    for (const std::string& s : samples) {\n        std::cout << \"[\" << s << \"] -> \"\n                  << (isPalindrome(s) ? \"yes\" : \"no\") << std::endl;\n    }\n    return 0;\n}",
];
