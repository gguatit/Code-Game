export const snippets = [
  "std::vector<int> v = {1, 2, 3};",
  'for (const auto& [key, value] : map) {',
  'auto* p = static_cast<int*>(malloc(n * sizeof(int)));',
  'std::sort(v.begin(), v.end(), std::greater<int>());',
  'printf("%d\\n", x);',
  'int len = sizeof(arr) / sizeof(arr[0]);',
];
