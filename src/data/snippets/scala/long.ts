export const snippets = [
  "def fib(n: Int): BigInt = {\n  var a: BigInt = 0\n  var b: BigInt = 1\n  for (_ <- 2 to n) {\n    val next = a + b\n    a = b\n    b = next\n  }\n  if (n < 2) n else b\n}\n\n@main def run(): Unit =\n  for (i <- 0 to 9) do\n    println(s\"fib($i) = ${fib(i)}\")",
  "def quicksort(items: List[Int]): List[Int] =\n  if items.length <= 1 then items\n  else\n    val pivot = items(items.length / 2)\n    val less = items.filter(_ < pivot)\n    val equal = items.filter(_ == pivot)\n    val greater = items.filter(_ > pivot)\n    quicksort(less) ++ equal ++ quicksort(greater)\n\n@main def run(): Unit =\n  println(quicksort(List(5, 2, 9, 1, 7, 3, 8, 2)))",
  "def binarySearch(sorted: Array[Int], target: Int): Int =\n  var lo = 0\n  var hi = sorted.length - 1\n  while lo <= hi do\n    val mid = (lo + hi) / 2\n    if sorted(mid) == target then return mid\n    else if sorted(mid) < target then lo = mid + 1\n    else hi = mid - 1\n  -1\n\n@main def run(): Unit =\n  println(binarySearch(Array(1, 3, 5, 7, 9), 7))",
  "def isPrime(n: Int): Boolean =\n  if n < 2 then false\n  else (2 to math.sqrt(n).toInt).forall(n % _ != 0)\n\ndef primesUpTo(limit: Int): Seq[Int] =\n  (2 to limit).filter(isPrime)\n\n@main def run(): Unit =\n  println(primesUpTo(30).mkString(\", \"))",
  "def isPalindrome(text: String): Boolean =\n  val clean = text.toLowerCase.filter(_.isLetterOrDigit)\n  clean == clean.reverse\n\n@main def run(): Unit =\n  println(isPalindrome(\"Never odd or even\"))\n  println(isPalindrome(\"hello world\"))",
];
