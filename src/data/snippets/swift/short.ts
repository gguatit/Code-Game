export const snippets = [
  "guard let url = URL(string: input) else { return }",
  "let total = items.reduce(0, +)",
  "names.map { $0.uppercased() }.sorted()",
  "if case .success(let value) = result { print(value) }",
  "label.text = user?.profile?.nickname ?? \"unknown\"",
  "defer { file.close() }",
];
