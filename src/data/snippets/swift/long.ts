export const snippets = [
  "func fib(_ n: Int) -> Int {\n    var a = 0\n    var b = 1\n    for _ in 0..<n {\n        let next = a + b\n        a = b\n        b = next\n    }\n    return a\n}\n\nfor n in [5, 10, 20] {\n    print(\"fib(\\(n)) = \\(fib(n))\")\n}",
  "func quicksort(_ items: [Int]) -> [Int] {\n    if items.count <= 1 { return items }\n    let pivot = items[0]\n    let rest = Array(items.dropFirst())\n    let left = rest.filter { $0 < pivot }\n    let right = rest.filter { $0 >= pivot }\n    return quicksort(left) + [pivot] + quicksort(right)\n}\n\nlet data = [42, 7, 19, 3, 88, 23]\nprint(quicksort(data))",
  "func binarySearch(_ items: [Int], _ target: Int) -> Int? {\n    var lo = 0\n    var hi = items.count - 1\n    while lo <= hi {\n        let mid = (lo + hi) / 2\n        if items[mid] == target { return mid }\n        if items[mid] < target { lo = mid + 1 }\n        else { hi = mid - 1 }\n    }\n    return nil\n}\n\nlet sorted = [1, 3, 5, 8, 13, 21, 34]\nprint(binarySearch(sorted, 13) ?? -1)\nprint(binarySearch(sorted, 4) ?? -1)",
  "func primesUpTo(_ limit: Int) -> [Int] {\n    var sieve = Array(repeating: true, count: limit + 1)\n    sieve[0] = false\n    if limit >= 1 { sieve[1] = false }\n    var result: [Int] = []\n    for i in 2...limit {\n        guard sieve[i] else { continue }\n        result.append(i)\n        var j = i * i\n        while j <= limit {\n            sieve[j] = false\n            j += i\n        }\n    }\n    return result\n}\n\nprint(primesUpTo(30))",
  "func isPalindrome(_ text: String) -> Bool {\n    let clean = text.lowercased().filter { $0.isLetter || $0.isNumber }\n    return clean == String(clean.reversed())\n}\n\nprint(isPalindrome(\"Never odd or even\"))\nprint(isPalindrome(\"Hello world\"))",
];
