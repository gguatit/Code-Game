export const snippets = [
  "const fibonacci = (count) => {\n  if (!Number.isInteger(count) || count < 0) {\n    throw new Error(\"count must be a non-negative integer\");\n  }\n  const sequence = [];\n  let a = 0;\n  let b = 1;\n  for (let i = 0; i < count; i++) {\n    sequence.push(a);\n    const next = a + b;\n    a = b;\n    b = next;\n  }\n  return sequence;\n};\n\nconst describeFibs = (fibs) => {\n  const head = fibs.slice(0, 5).join(\", \");\n  const tail = fibs[fibs.length - 1];\n  return `starts [${head}] and ends at ${tail}`;\n};\n\nconst fibonacciDemo = () => {\n  const n = 15;\n  const fibs = fibonacci(n);\n  console.log(`First ${n} Fibonacci numbers:`);\n  console.log(fibs.join(\" \"));\n  console.log(describeFibs(fibs));\n};\n\nfibonacciDemo();",
  "const quicksort = (items) => {\n  if (items.length <= 1) {\n    return items;\n  }\n  const pivotIndex = Math.floor(items.length / 2);\n  const pivot = items[pivotIndex];\n  const less = [];\n  const equal = [];\n  const greater = [];\n  for (const item of items) {\n    if (item < pivot) {\n      less.push(item);\n    } else if (item === pivot) {\n      equal.push(item);\n    } else {\n      greater.push(item);\n    }\n  }\n  return [...quicksort(less), ...equal, ...quicksort(greater)];\n};\n\nconst sortDemo = () => {\n  const data = [42, 7, 19, 3, 88, 25, 1, 56, 12, 70];\n  console.log(\"before:\", data.join(\", \"));\n  const sorted = quicksort(data);\n  console.log(\"after:\", sorted.join(\", \"));\n  const words = [\"pear\", \"fig\", \"apple\", \"kiwi\", \"date\"];\n  console.log(\"words:\", quicksort(words).join(\", \"));\n  const empty = quicksort([]);\n  console.log(\"empty stays empty:\", empty.length === 0);\n};\n\nsortDemo();",
  "const binarySearch = (sorted, target) => {\n  let low = 0;\n  let high = sorted.length - 1;\n  while (low <= high) {\n    const mid = Math.floor((low + high) / 2);\n    const value = sorted[mid];\n    if (value === target) {\n      return mid;\n    } else if (value < target) {\n      low = mid + 1;\n    } else {\n      high = mid - 1;\n    }\n  }\n  return -1;\n};\n\nconst report = (values, target) => {\n  const index = binarySearch(values, target);\n  if (index === -1) {\n    return `${target} is not present`;\n  }\n  return `${target} found at index ${index}`;\n};\n\nconst searchDemo = () => {\n  const values = [2, 5, 8, 12, 16, 23, 38, 56, 72, 91];\n  console.log(report(values, 23));\n  console.log(report(values, 91));\n  console.log(report(values, 4));\n};\n\nsearchDemo();",
  "const sieveOfEratosthenes = (limit) => {\n  const isPrime = new Array(limit + 1).fill(true);\n  isPrime[0] = false;\n  isPrime[1] = false;\n  for (let number = 2; number * number <= limit; number++) {\n    if (isPrime[number]) {\n      for (let multiple = number * number; multiple <= limit; multiple += number) {\n        isPrime[multiple] = false;\n      }\n    }\n  }\n  const primes = [];\n  for (let i = 0; i <= limit; i++) {\n    if (isPrime[i]) {\n      primes.push(i);\n    }\n  }\n  return primes;\n};\n\nconst sieveDemo = () => {\n  const primes = sieveOfEratosthenes(50);\n  console.log(\"primes up to 50:\");\n  console.log(primes.join(\", \"));\n  console.log(\"count:\", primes.length);\n  console.log(\"largest:\", primes[primes.length - 1]);\n};\n\nsieveDemo();",
  "const cleanText = (text) => {\n  return text.toLowerCase().replace(/[^a-z0-9]/g, \"\");\n};\n\nconst reverseText = (text) => {\n  return text.split(\"\").reverse().join(\"\");\n};\n\nconst isPalindrome = (text) => {\n  const cleaned = cleanText(text);\n  if (cleaned.length === 0) {\n    return false;\n  }\n  return cleaned === reverseText(cleaned);\n};\n\nconst check = (candidate) => {\n  const verdict = isPalindrome(candidate) ? \"yes\" : \"no\";\n  console.log(`${candidate} -> palindrome? ${verdict}`);\n};\n\nconst palindromeDemo = () => {\n  check(\"A man, a plan, a canal: Panama\");\n  check(\"Was it a car or a cat I saw?\");\n  check(\"Hello, world!\");\n  check(\"\");\n  check(\"12321\");\n};\n\npalindromeDemo();",
];
