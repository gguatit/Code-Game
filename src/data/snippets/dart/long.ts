export const snippets = [
  "List<int> fibonacci(int count) {\n  if (count <= 0) return [];\n  final terms = <int>[0];\n  var prev = 0;\n  var curr = 1;\n  while (terms.length < count) {\n    terms.add(curr);\n    final next = prev + curr;\n    prev = curr;\n    curr = next;\n  }\n  return terms;\n}\n\nvoid main() {\n  const n = 15;\n  final terms = fibonacci(n);\n  print('First $n Fibonacci numbers:');\n  for (final term in terms) {\n    print(term);\n  }\n  final joined = terms.join(', ');\n  print('Compact view: $joined');\n  final big = fibonacci(30).last;\n  print('Term number 30 is $big');\n  final sum = terms.fold<int>(0, (a, b) => a + b);\n  print('Sum of the first $n terms: $sum');\n}",
  "List<int> quickSort(List<int> items) {\n  if (items.length <= 1) return items;\n  final pivot = items[items.length ~/ 2];\n  final less = <int>[];\n  final equal = <int>[];\n  final greater = <int>[];\n  for (final item in items) {\n    if (item < pivot) {\n      less.add(item);\n    } else if (item == pivot) {\n      equal.add(item);\n    } else {\n      greater.add(item);\n    }\n  }\n  return [...quickSort(less), ...equal, ...quickSort(greater)];\n}\n\nvoid main() {\n  const numbers = [42, 7, 19, 3, 88, 1, 56, 23, 9, 71, 5];\n  print('Input list: $numbers');\n  final sorted = quickSort(numbers);\n  print('Sorted list: $sorted');\n  final median = sorted[sorted.length ~/ 2];\n  print('Middle element: $median');\n  final indexed = [\n    for (var i = 0; i < sorted.length; i++) '$i:${sorted[i]}',\n  ];\n  print('Indexed view: ${indexed.join(' ')}');\n}",
  "int binarySearch(List<int> sorted, int target) {\n  var low = 0;\n  var high = sorted.length - 1;\n  while (low <= high) {\n    final mid = low + (high - low) ~/ 2;\n    final value = sorted[mid];\n    if (value == target) {\n      return mid;\n    } else if (value < target) {\n      low = mid + 1;\n    } else {\n      high = mid - 1;\n    }\n  }\n  return -1;\n}\n\nvoid main() {\n  const data = [2, 5, 8, 12, 16, 23, 38, 56, 72, 91];\n  const targets = [23, 91, 1, 38];\n  print('Data: $data');\n  for (final target in targets) {\n    final index = binarySearch(data, target);\n    if (index == -1) {\n      print('$target was not found in the list.');\n    } else {\n      print('$target found at index $index');\n    }\n  }\n}",
  "List<int> sieveOfEratosthenes(int limit) {\n  if (limit < 2) return [];\n  final isPrime = List<bool>.filled(limit + 1, true);\n  isPrime[0] = false;\n  isPrime[1] = false;\n  for (var i = 2; i * i <= limit; i++) {\n    if (isPrime[i]) {\n      for (var j = i * i; j <= limit; j += i) {\n        isPrime[j] = false;\n      }\n    }\n  }\n  final primes = <int>[];\n  for (var i = 2; i <= limit; i++) {\n    if (isPrime[i]) {\n      primes.add(i);\n    }\n  }\n  return primes;\n}\n\nvoid main() {\n  const limit = 100;\n  final primes = sieveOfEratosthenes(limit);\n  print('${primes.length} primes up to $limit:');\n  print(primes.join(', '));\n  final firstTen = primes.sublist(0, 10);\n  print('First ten: ${firstTen.join(' ')}');\n  final largest = primes.last;\n  print('Largest prime not exceeding $limit is $largest.');\n}",
  "bool isPalindrome(String text) {\n  final pattern = RegExp('[^a-z0-9]');\n  final cleaned = text.toLowerCase().replaceAll(pattern, '');\n  var left = 0;\n  var right = cleaned.length - 1;\n  while (left < right) {\n    if (cleaned[left] != cleaned[right]) {\n      return false;\n    }\n    left++;\n    right--;\n  }\n  return true;\n}\n\nvoid main() {\n  const samples = [\n    'A man, a plan, a canal: Panama',\n    'Was it a car or a cat I saw?',\n    'racecar',\n    'Hello, world!',\n    'No lemon, no melon',\n  ];\n  for (final sample in samples) {\n    final verdict = isPalindrome(sample) ? 'YES' : 'NO';\n    print('$sample -> palindrome: $verdict');\n  }\n  final palindromes = samples.where(isPalindrome).length;\n  print('$palindromes of ${samples.length} samples passed.');\n}",
];
