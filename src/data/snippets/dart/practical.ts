export const snippets = [
  "Map<String, int> wordFrequency(String text) {\n  final counts = <String, int>{};\n  for (final word in text.toLowerCase().split(RegExp('\\\\W+'))) {\n    if (word.isEmpty) continue;\n    counts[word] = (counts[word] ?? 0) + 1;\n  }\n  return counts;\n}\n\nvoid main() {\n  final freq = wordFrequency('the cat and the dog and the bird');\n  for (final e in freq.entries) {\n    print('${e.key}: ${e.value}');\n  }\n}",
  "class User {\n  final String email;\n  final String password;\n  User(this.email, this.password);\n\n  bool get valid {\n    final emailOk = RegExp(r'^\\S+@\\S+\\.\\S+$').hasMatch(email);\n    return emailOk && password.length >= 8;\n  }\n}\n\nvoid main() {\n  print(User('a@b.com', 'longenough').valid);\n  print(User('bad', 'short').valid);\n}",
  "double mean(List<double> xs) =>\n    xs.isEmpty ? 0 : xs.reduce((a, b) => a + b) / xs.length;\n\ndouble variance(List<double> xs) {\n  final m = mean(xs);\n  var total = 0.0;\n  for (final x in xs) {\n    total += (x - m) * (x - m);\n  }\n  return total / xs.length;\n}\n\nvoid describe(List<double> xs) {\n  print('mean: ${mean(xs)}');\n  print('stddev: ${variance(xs)}');\n}",
  "bool isBalanced(String text) {\n  final stack = <String>[];\n  final pairs = {')': '(', ']': '[', '}': '{'};\n  for (final ch in text.split('')) {\n    if ('([{'.contains(ch)) {\n      stack.add(ch);\n    } else if (pairs.containsKey(ch)) {\n      if (stack.isEmpty || stack.removeLast() != pairs[ch]) {\n        return false;\n      }\n    }\n  }\n  return stack.isEmpty;\n}\n\nvoid main() {\n  print(isBalanced('{[()]}'));\n  print(isBalanced('{[(])}'));\n}",
];
