export const snippets = [
  "local sentence = \"the quick brown fox jumps over the lazy dog \" ..\n  \"while the dog naps and the fox watches the quick bird\"\n\nlocal counts = {}\nfor word in sentence:gmatch(\"%a+\") do\n  counts[word] = (counts[word] or 0) + 1\nend\n\nlocal words = {}\nfor word in pairs(counts) do\n  words[#words + 1] = word\nend\ntable.sort(words)\n\nprint(\"Word frequencies:\")\nfor _, word in ipairs(words) do\n  print(string.format(\"%-8s : %d\", word, counts[word]))\nend\n\nlocal total = 0\nfor _, word in ipairs(words) do\n  total = total + counts[word]\nend\nprint(string.format(\"Unique words: %d | Total words: %d\", #words, total))\n\nlocal most, best = \"\", 0\nfor _, word in ipairs(words) do\n  if counts[word] > best then\n    most, best = word, counts[word]\n  end\nend\nprint(string.format(\"Most common: '%s' appears %d times\", most, best))",
  "local function valid_username(name)\n  if #name < 3 or #name > 16 then\n    return false, \"length must be 3-16 characters\"\n  elseif not name:match(\"^%a[%w_]*$\") then\n    return false, \"letter first, then letters, digits or _\"\n  end\n  return true, \"ok\"\nend\n\nlocal function valid_password(pass)\n  if #pass < 8 then\n    return false, \"needs at least 8 characters\"\n  elseif not pass:match(\"%d\") then\n    return false, \"must include a digit\"\n  elseif not pass:match(\"%u\") then\n    return false, \"must include an uppercase letter\"\n  elseif pass:find(\"%s\") then\n    return false, \"must not contain spaces\"\n  end\n  return true, \"ok\"\nend\n\nlocal function try(label, value, checker)\n  local ok, note = checker(value)\n  print(string.format(\"%-5s %-16s %s (%s)\", label, value, ok and \"PASS\" or \"FAIL\", note))\nend\n\ntry(\"user\", \"ab\", valid_username)\ntry(\"user\", \"bad name\", valid_username)\ntry(\"pass\", \"short1\", valid_password)\ntry(\"pass\", \"nouppercase9x\", valid_password)\ntry(\"pass\", \"Solid2026Key\", valid_password)",
  "local function describe(numbers)\n  local stats = {}\n  local sorted = {table.unpack(numbers)}\n  table.sort(sorted)\n  stats.min = sorted[1]\n  stats.max = sorted[#sorted]\n  local total = 0\n  for _, value in ipairs(numbers) do\n    total = total + value\n  end\n  stats.mean = total / #numbers\n  local mid = math.ceil(#sorted / 2)\n  if #sorted % 2 == 1 then\n    stats.median = sorted[mid]\n  else\n    stats.median = (sorted[mid] + sorted[mid + 1]) / 2\n  end\n  return stats\nend\n\nlocal function report(title, numbers)\n  local s = describe(numbers)\n  print(string.format(\"%s (n=%d)\", title, #numbers))\n  print(string.format(\"  min=%.2f  max=%.2f\", s.min, s.max))\n  print(string.format(\"  mean=%.2f  median=%.2f\", s.mean, s.median))\nend\n\nreport(\"odd set\", {7, 3, 9, 1, 5})\nreport(\"even set\", {8, 2, 6, 4})\nreport(\"single\", {42})",
  "local PAIRS = {\"]\" = \"[\", [\"}\"] = \"{\", [\")\"] = \"(\"}\n\nlocal function is_balanced(text)\n  local stack = {}\n  for char in text:gmatch(\".\") do\n    if char == \"[\" or char == \"{\" or char == \"(\" then\n      stack[#stack + 1] = char\n    elseif PAIRS[char] then\n      if stack[#stack] ~= PAIRS[char] then\n        return false\n      end\n      stack[#stack] = nil\n    end\n  end\n  return #stack == 0\nend\n\nlocal samples = {\n  \"{[()]}\",\n  \"([)]\",\n  \"((()))\",\n  \"()[]{}\",\n  \"(((((\",\n  \"a(b[c]d)e\",\n  \")(\",\n}\n\nfor _, text in ipairs(samples) do\n  local verdict = is_balanced(text) and \"balanced\" or \"unbalanced\"\n  print(string.format(\"%-12s -> %s\", text, verdict))\nend",
];
