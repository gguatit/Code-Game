export const snippets = [
  "local function fib(n)\n  if n < 2 then\n    return n\n  end\n  local a, b = 0, 1\n  for i = 2, n do\n    a, b = b, a + b\n  end\n  return b\nend\n\nfor i = 0, 9 do\n  print(string.format(\"fib(%d) = %d\", i, fib(i)))\nend",
  "local function appendAll(dst, src)\n  for _, v in ipairs(src) do\n    table.insert(dst, v)\n  end\nend\n\nlocal function quicksort(t)\n  if #t <= 1 then return t end\n  local pivot = t[math.floor(#t / 2) + 1]\n  local less, equal, greater = {}, {}, {}\n  for _, x in ipairs(t) do\n    if x < pivot then table.insert(less, x)\n    elseif x > pivot then table.insert(greater, x)\n    else table.insert(equal, x) end\n  end\n  local result = {}\n  appendAll(result, quicksort(less))\n  appendAll(result, equal)\n  appendAll(result, quicksort(greater))\n  return result\nend",
  "local function binarySearch(sorted, target)\n  local lo, hi = 1, #sorted\n  while lo <= hi do\n    local mid = math.floor((lo + hi) / 2)\n    if sorted[mid] == target then\n      return mid\n    elseif sorted[mid] < target then\n      lo = mid + 1\n    else\n      hi = mid - 1\n    end\n  end\n  return -1\nend\n\nprint(binarySearch({1, 3, 5, 7, 9}, 7))",
  "local function isPrime(n)\n  if n < 2 then return false end\n  for i = 2, math.floor(math.sqrt(n)) do\n    if n % i == 0 then return false end\n  end\n  return true\nend\n\nlocal function primesUpTo(limit)\n  local out = {}\n  for n = 2, limit do\n    if isPrime(n) then table.insert(out, n) end\n  end\n  return out\nend\n\nprint(table.concat(primesUpTo(30), \", \"))",
  "local function isPalindrome(text)\n  local clean = text:lower():gsub(\"[^%w]\", \"\")\n  local chars = {}\n  for ch in clean:gmatch(\".\") do\n    table.insert(chars, ch)\n  end\n  for i = 1, #chars // 2 do\n    if chars[i] ~= chars[#chars - i + 1] then\n      return false\n    end\n  end\n  return true\nend",
];
