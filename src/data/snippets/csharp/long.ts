export const snippets = [
  "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        int n = 12;\n        long[] fib = Fibonacci(n);\n        Console.WriteLine(\"The first \" + n + \" Fibonacci numbers are:\");\n        for (int i = 0; i < fib.Length; i++) {\n            Console.Write(fib[i] + (i < fib.Length - 1 ? \", \" : \"\\n\"));\n        }\n        long sum = 0;\n        foreach (long f in fib)\n        {\n            sum += f;\n        }\n        Console.WriteLine(\"Their sum is: \" + sum);\n    }\n\n    static long[] Fibonacci(int count)\n    {\n        long[] seq = new long[Math.Max(count, 0)];\n        if (seq.Length > 0) { seq[0] = 0; }\n        if (seq.Length > 1) { seq[1] = 1; }\n        for (int i = 2; i < seq.Length; i++) {\n            seq[i] = seq[i - 1] + seq[i - 2];\n        }\n        return seq;\n    }\n}",
  "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        int[] data = { 42, 7, 19, 3, 88, 25, 56, 1, 70, 13 };\n        Console.WriteLine(\"Before: \" + string.Join(\" \", data));\n        QuickSort(data, 0, data.Length - 1);\n        Console.WriteLine(\"After:  \" + string.Join(\" \", data));\n    }\n\n    static void QuickSort(int[] arr, int low, int high)\n    {\n        if (low >= high) return;\n        int p = Partition(arr, low, high);\n        QuickSort(arr, low, p - 1);\n        QuickSort(arr, p + 1, high);\n    }\n\n    static int Partition(int[] arr, int low, int high)\n    {\n        int pivot = arr[high];\n        int i = low - 1;\n        for (int j = low; j < high; j++) {\n            if (arr[j] <= pivot) { i++; (arr[i], arr[j]) = (arr[j], arr[i]); }\n        }\n        (arr[i + 1], arr[high]) = (arr[high], arr[i + 1]);\n        return i + 1;\n    }\n}",
  "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        int[] sorted = { 2, 5, 8, 12, 16, 23, 38, 56, 72, 91 };\n        int[] targets = { 23, 2, 91, 50, 8 };\n        foreach (int target in targets) {\n            int index = BinarySearch(sorted, target);\n            if (index >= 0) {\n                Console.WriteLine(target + \" found at index \" + index);\n            } else {\n                Console.WriteLine(target + \" was not found\");\n            }\n        }\n    }\n\n    static int BinarySearch(int[] arr, int target)\n    {\n        int low = 0;\n        int high = arr.Length - 1;\n        while (low <= high) {\n            int mid = low + (high - low) / 2;\n            if (arr[mid] == target) { return mid; }\n            else if (arr[mid] < target) { low = mid + 1; }\n            else { high = mid - 1; }\n        }\n        return -1;\n    }\n}",
  "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        int limit = 100;\n        bool[] isComposite = Sieve(limit);\n        int count = 0;\n        Console.WriteLine(\"Primes up to \" + limit + \":\");\n        for (int n = 2; n <= limit; n++) {\n            if (!isComposite[n]) {\n                Console.Write(n + \" \");\n                count++;\n            }\n        }\n        Console.WriteLine();\n        Console.WriteLine(\"Total primes found: \" + count);\n    }\n\n    static bool[] Sieve(int limit)\n    {\n        bool[] composite = new bool[limit + 1];\n        for (int p = 2; p * p <= limit; p++) {\n            if (!composite[p]) {\n                for (int m = p * p; m <= limit; m += p) {\n                    composite[m] = true;\n                }\n            }\n        }\n        return composite;\n    }\n}",
  "using System;\n\nclass Program\n{\n    static void Main()\n    {\n        string[] samples = {\n            \"A man, a plan, a canal: Panama\",\n            \"racecar\",\n            \"Hello, World!\",\n            \"No 'x' in Nixon\"\n        };\n        foreach (string s in samples) {\n            string verdict = IsPalindrome(s) ? \"IS\" : \"is NOT\";\n            Console.WriteLine(\"\\\"\" + s + \"\\\" \" + verdict + \" a palindrome\");\n        }\n    }\n\n    static bool IsPalindrome(string text)\n    {\n        int left = 0;\n        int right = text.Length - 1;\n        while (left < right) {\n            while (left < right && !char.IsLetterOrDigit(text[left])) left++;\n            while (left < right && !char.IsLetterOrDigit(text[right])) right--;\n            if (char.ToLower(text[left]) != char.ToLower(text[right])) return false;\n            left++;\n            right--;\n        }\n        return true;\n    }\n}",
];
