export const snippets = [
  "using System;\n\nclass Fibonacci\n{\n    static long Fib(int n)\n    {\n        long a = 0;\n        long b = 1;\n        for (int i = 0; i < n; i++)\n        {\n            long next = a + b;\n            a = b;\n            b = next;\n        }\n        return a;\n    }\n\n    static void Main()\n    {\n        int[] sizes = { 5, 10, 20 };\n        foreach (int n in sizes)\n        {\n            Console.WriteLine($\"fib({n}) = {Fib(n)}\");\n        }\n    }\n}",
  "using System;\nusing System.Collections.Generic;\n\nclass Quicksort\n{\n    static List<int> Sort(List<int> items)\n    {\n        if (items.Count <= 1) return items;\n        int pivot = items[0];\n        var left = new List<int>();\n        var right = new List<int>();\n        for (int i = 1; i < items.Count; i++)\n        {\n            if (items[i] < pivot) left.Add(items[i]);\n            else right.Add(items[i]);\n        }\n        var result = new List<int>(Sort(left)) { pivot };\n        result.AddRange(Sort(right));\n        return result;\n    }\n}",
  "using System;\n\nclass BinarySearch\n{\n    static int Search(int[] items, int target)\n    {\n        int lo = 0;\n        int hi = items.Length - 1;\n        while (lo <= hi)\n        {\n            int mid = (lo + hi) / 2;\n            if (items[mid] == target) return mid;\n            if (items[mid] < target) lo = mid + 1;\n            else hi = mid - 1;\n        }\n        return -1;\n    }\n\n    static void Main()\n    {\n        int[] sorted = { 1, 3, 5, 8, 13, 21, 34 };\n        Console.WriteLine(Search(sorted, 13));\n        Console.WriteLine(Search(sorted, 4));\n    }\n}",
  "using System;\n\nclass PrimeSieve\n{\n    static void Main()\n    {\n        int limit = 30;\n        bool[] sieve = new bool[limit + 1];\n        for (int i = 2; i <= limit; i++) sieve[i] = true;\n        for (int i = 2; (long)i * i <= limit; i++)\n        {\n            if (!sieve[i]) continue;\n            for (int j = i * i; j <= limit; j += i) sieve[j] = false;\n        }\n        for (int i = 2; i <= limit; i++)\n        {\n            if (sieve[i]) Console.Write($\"{i} \");\n        }\n    }\n}",
  "using System;\nusing System.Linq;\n\nclass Palindrome\n{\n    static bool IsPalindrome(string text)\n    {\n        string clean = new string(text.ToLower().Where(char.IsLetterOrDigit).ToArray());\n        int left = 0;\n        int right = clean.Length - 1;\n        while (left < right)\n        {\n            if (clean[left] != clean[right]) return false;\n            left++;\n            right--;\n        }\n        return true;\n    }\n\n    static void Main()\n    {\n        Console.WriteLine(IsPalindrome(\"Never odd or even\"));\n        Console.WriteLine(IsPalindrome(\"Hello world\"));\n    }\n}",
];
