export const snippets = [
  "use strict;\nuse warnings;\n\nsub fib {\n    my ($n) = @_;\n    return $n if $n < 2;\n    my ($a, $b) = (0, 1);\n    for (2 .. $n) {\n        ($a, $b) = ($b, $a + $b);\n    }\n    return $b;\n}\n\nfor my $i (0 .. 9) {\n    print \"fib($i) = \", fib($i), \"\\n\";\n}",
  "use strict;\nuse warnings;\n\nsub quicksort {\n    my @items = @_;\n    return @items if @items <= 1;\n    my $pivot = $items[0];\n    my @less = grep { $_ < $pivot } @items[1 .. $#items];\n    my @greater = grep { $_ >= $pivot } @items[1 .. $#items];\n    return quicksort(@less), $pivot, quicksort(@greater);\n}\n\nprint join(\", \", quicksort(5, 2, 9, 1, 7, 3, 8, 2)), \"\\n\";",
  "use strict;\nuse warnings;\n\nsub binary_search {\n    my ($sorted, $target) = @_;\n    my ($lo, $hi) = (0, $#$sorted);\n    while ($lo <= $hi) {\n        my $mid = int(($lo + $hi) / 2);\n        if ($sorted->[$mid] == $target) { return $mid; }\n        elsif ($sorted->[$mid] < $target) { $lo = $mid + 1; }\n        else { $hi = $mid - 1; }\n    }\n    return -1;\n}\n\nprint binary_search([1, 3, 5, 7, 9], 7), \"\\n\";",
  "use strict;\nuse warnings;\n\nsub is_prime {\n    my ($n) = @_;\n    return 0 if $n < 2;\n    for (my $i = 2; $i * $i <= $n; $i++) {\n        return 0 if $n % $i == 0;\n    }\n    return 1;\n}\n\nmy @primes = grep { is_prime($_) } 2 .. 30;\nprint join(\", \", @primes), \"\\n\";",
  "use strict;\nuse warnings;\n\nsub is_palindrome {\n    my ($text) = @_;\n    my $clean = lc $text;\n    $clean =~ s/[^a-z0-9]//g;\n    return $clean eq reverse $clean;\n}\n\nprint is_palindrome(\"Never odd or even\") ? \"yes\" : \"no\", \"\\n\";\nprint is_palindrome(\"hello world\") ? \"yes\" : \"no\", \"\\n\";",
];
