export const snippets = [
  "package main\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n)\n\nfunc main() {\n\ttext := \"the quick brown fox jumps over the lazy dog\"\n\tcounts := make(map[string]int)\n\tfor _, word := range strings.Fields(text) {\n\t\tcounts[word]++\n\t}\n\tfor word, n := range counts {\n\t\tfmt.Println(word, n)\n\t}\n}",
  "package main\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n)\n\ntype user struct {\n\temail    string\n\tpassword string\n\tage      int\n}\n\nfunc validate(u user) error {\n\tif !strings.Contains(u.email, \"@\") {\n\t\treturn fmt.Errorf(\"invalid email\")\n\t}\n\tif len(u.password) < 8 {\n\t\treturn fmt.Errorf(\"password too short\")\n\t}\n\tif u.age < 18 {\n\t\treturn fmt.Errorf(\"must be an adult\")\n\t}\n\treturn nil\n}",
  "package main\n\nimport \"fmt\"\n\nfunc describe(nums []int) (int, int, float64) {\n\tmin, max := nums[0], nums[0]\n\tsum := 0\n\tfor _, n := range nums {\n\t\tif n < min {\n\t\t\tmin = n\n\t\t}\n\t\tif n > max {\n\t\t\tmax = n\n\t\t}\n\t\tsum += n\n\t}\n\tmean := float64(sum) / float64(len(nums))\n\treturn min, max, mean\n}\n\nfunc main() {\n\tmin, max, mean := describe([]int{4, 8, 15, 16, 23, 42})\n\tfmt.Println(min, max, mean)\n}",
  "package main\n\nimport \"fmt\"\n\nfunc isBalanced(source string) bool {\n\tvar stack []rune\n\tpairs := map[rune]rune{')': '(', ']': '[', '}': '{'}\n\tfor _, ch := range source {\n\t\tswitch ch {\n\t\tcase '(', '[', '{':\n\t\t\tstack = append(stack, ch)\n\t\tcase ')', ']', '}':\n\t\t\tif len(stack) == 0 || stack[len(stack)-1] != pairs[ch] {\n\t\t\t\treturn false\n\t\t\t}\n\t\t\tstack = stack[:len(stack)-1]\n\t\t}\n\t}\n\treturn len(stack) == 0\n}",
];
