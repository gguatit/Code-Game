export const snippets = [
  "-- | Print the most frequent words in a small text corpus.\n--\n-- The classic data pipeline in miniature: normalize the corpus\n-- into lowercase tokens, aggregate them into a strict Map, rank\n-- the entries by descending count, then render the head of the\n-- ranking. Every stage is a small total function, which keeps\n-- the whole program easy to test one piece at a time.\nmodule Main where\n\nimport Data.Char (toLower)\nimport Data.List (sortBy)\nimport Data.Map.Strict (Map)\nimport qualified Data.Map.Strict as Map\nimport Data.Ord (Down (Down), comparing)\n\n-- | Toy corpus so the demo runs without reading any files.\nsampleText :: String\nsampleText =\n  unwords\n    [ \"haskell is a purely functional language with a strong static type system\"\n    , \"pure functions always return the same output for the same input\"\n    , \"lazy evaluation means values are computed only when they are needed\"\n    , \"immutable data makes it easier to reason about what programs do\"\n    , \"the compiler uses types to catch whole classes of bugs early\"\n    ]\n\n-- | Lowercase every whitespace-separated word of the raw text.\ntokenize :: String -> [String]\ntokenize = map (map toLower) . words\n\n-- | Tally tokens into a table; fromListWith combines duplicates\n-- with (+), so repeated words accumulate instead of colliding.\nwordCounts :: String -> Map String Int\nwordCounts text = Map.fromListWith (+) [(w, 1) | w <- tokenize text]\n\n-- | Sort entries by descending count and keep only the first n.\ntopN :: Int -> Map String Int -> [(String, Int)]\ntopN n counts =\n  take n (sortBy (comparing (Down . snd)) (Map.toList counts))\n\n-- | Report corpus size, then print the ranked winners.\nmain :: IO ()\nmain = do\n  let counts = wordCounts sampleText\n      ranking = topN 5 counts\n  putStrLn (\"corpus holds \" ++ show (Map.size counts) ++ \" distinct words\")\n  putStrLn \"ranked by frequency:\"\n  mapM_ print ranking",
  "-- | Validate sign-up fields; each rule set answers with complaint strings.\nmodule Main where\n\nimport Data.Char (isAlpha, isAlphaNum, isDigit)\nimport Data.List (intercalate)\n\n-- | Usernames are short slugs built from letters and digits only.\nusernameErrors :: String -> [String]\nusernameErrors name\n  | not (all isAlphaNum name) = [\"only letters and digits are allowed\"]\n  | length name < 3 || length name > 16 = [\"length must be between 3 and 16\"]\n  | otherwise = []\n\n-- | Emails need exactly one @ with text on both sides of it.\nemailErrors :: String -> [String]\nemailErrors email\n  | length (filter (== '@') email) /= 1 = [\"exactly one @ is required\"]\n  | null local || null domain = [\"text is required around the @\"]\n  | otherwise = []\n  where\n    local = takeWhile (/= '@') email\n    domain = drop 1 . dropWhile (/= '@') $ email\n\n-- | Passwords need enough length plus digit and letter coverage.\npasswordErrors :: String -> [String]\npasswordErrors password =\n  concat\n    [ [\"at least 8 characters are required\" | length password < 8]\n    , [\"add at least one digit\" | not (any isDigit password)]\n    , [\"add at least one letter\" | not (any isAlpha password)]\n    ]\n\n-- | Split the form line into fields and label every complaint.\nvalidateUser :: String -> [String]\nvalidateUser line = case words line of\n  [name, email, password] ->\n    map (\"username: \" ++) (usernameErrors name)\n      ++ map (\"email: \" ++) (emailErrors email)\n      ++ map (\"password: \" ++) (passwordErrors password)\n  _ -> [\"expected three fields: username email password\"]\n\n-- | Try a few submissions and print one verdict per line.\nmain :: IO ()\nmain = mapM_ (putStrLn . verdict) submissions\n  where\n    submissions =\n      [ \"ada lovelace ada@lovelace.dev short1\"\n      , \"grace hopper g.hopper@navy.mil Sea2Shore3x\"\n      , \"edsger ew@fp.club TempPass9\"\n      ]\n    verdict submission = case validateUser submission of\n      [] -> submission ++ \"  =>  accepted\"\n      errs -> submission ++ \"  =>  rejected: \" ++ intercalate \"; \" errs",
  "-- | Summarise a numeric sample: centre, spread, and range.\nmodule Main where\n\nimport Data.List (sort)\nimport Text.Printf (printf)\n\n-- | Field names read like getters at the call site.\ndata Stats = Stats\n  { mean :: Double\n  , median :: Double\n  , stdDev :: Double\n  , minV :: Double\n  , maxV :: Double\n  }\n\n-- | Middle element, or the average of the two central elements.\nmedianOf :: [Double] -> Double\nmedianOf xs =\n  let ordered = sort xs\n      size = length ordered\n      half = size `div` 2\n   in if odd size\n        then ordered !! half\n        else (ordered !! (half - 1) + ordered !! half) / 2\n\n-- | Nothing flags the empty sample; spread is population based.\ncomputeStats :: [Double] -> Maybe Stats\ncomputeStats xs\n  | null xs = Nothing\n  | otherwise =\n      let n = fromIntegral (length xs)\n          m = sum xs / n\n          var = sum [(x - m) * (x - m) | x <- xs] / n\n       in Just Stats\n            { mean = m\n            , median = medianOf xs\n            , stdDev = sqrt var\n            , minV = minimum xs\n            , maxV = maximum xs\n            }\n\n-- | Exercise assorted samples, including the empty edge case.\nmain :: IO ()\nmain = mapM_ present [[2, 4, 4, 4, 5, 5, 7, 9], [10, 20, 30], []]\n  where\n    present sample = do\n      putStrLn (\"sample \" ++ show sample)\n      putStrLn (maybe \"  (empty sample)\" digest (computeStats sample))\n    digest s =\n      printf \"mean=%.2f median=%.2f stddev=%.2f min=%.2f max=%.2f\"\n        (mean s) (median s) (stdDev s)\n        (minV s) (maxV s)",
  "-- | Verify that (), [] and {} nest correctly in one pass.\nmodule Main where\n\nimport Data.List (foldl')\n\n-- | Every bracket pair this checker understands.\npairs :: [(Char, Char)]\npairs = [('(', ')'), ('[', ']'), ('{', '}')]\n\n-- | The closing half of an opening bracket, if it has one.\ncloserFor :: Char -> Maybe Char\ncloserFor opener = lookup opener pairs\n\n-- | Result for a finished line: clean, or broken at an index.\ndata Verdict = Balanced | BrokenAt Int\n  deriving (Eq, Show)\n\n-- | One fold step: push the expected closer, or match and pop.\nstep :: ([(Char, Int)], Maybe Int) -> (Int, Char) -> ([(Char, Int)], Maybe Int)\nstep acc@(_, Just _) _ = acc\nstep (stack, Nothing) (index, ch)\n  | Just expect <- closerFor ch = ((expect, index) : stack, Nothing)\n  | ch `elem` \")]}\" =\n      case stack of\n        ((expect, _) : rest) | expect == ch -> (rest, Nothing)\n        _ -> (stack, Just index)\n  | otherwise = (stack, Nothing)\n\n-- | Scan a whole line and summarise how the scan ended.\ncheckLine :: String -> Verdict\ncheckLine line =\n  let (_, violation) = foldl' step ([], Nothing) (zip [0 ..] line)\n   in maybe Balanced BrokenAt violation\n\n-- | Human-readable phrasing for either outcome.\ndescribe :: Verdict -> String\ndescribe Balanced = \"balanced\"\ndescribe (BrokenAt i) = \"broken at index \" ++ show i\n\n-- | Run assorted samples, including deliberate mismatches.\nmain :: IO ()\nmain = mapM_ present samples\n  where\n    present line = putStrLn (line ++ \"  =>  \" ++ describe (checkLine line))\n    samples =\n      [ \"print (sum [x * 2 | x <- xs])\"\n      , \"data Tree = Leaf Int | Node Tree Tree\"\n      , \"pair (a, b] = b\"\n      , \")lonely(\"\n      ]"
];
