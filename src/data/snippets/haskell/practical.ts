export const snippets = [
  "module Main where\n\nimport Data.Char (toLower)\nimport Data.List (sortBy)\nimport qualified Data.Map as Map\nimport Data.Ord (comparing, Down (..))\n\ntext :: String\ntext =\n  \"the quick brown fox jumps over the lazy dog \"\n    ++ \"the dog barks and the fox runs away\"\n\nfrequency :: String -> Map.Map String Int\nfrequency s =\n  Map.fromListWith (+) [(w, 1) | w <- words (map toLower s)]\n\ntopCounts :: Int -> Map.Map String Int -> [(String, Int)]\ntopCounts k m =\n  take k (sortBy (comparing (Down . snd)) (Map.toList m))\n\nmain :: IO ()\nmain = do\n  let freq = frequency text\n  let total = sum (map snd (Map.toList freq))\n  putStrLn \"Word frequencies:\"\n  mapM_ (\\(w, c) ->\n    putStrLn (w ++ \": \" ++ replicate c '*')) (Map.toList freq)\n  putStrLn \"\"\n  putStrLn \"Top 3 most frequent:\"\n  mapM_ print (topCounts 3 freq)\n  putStrLn (\"Distinct words: \" ++ show (Map.size freq))\n  putStrLn (\"Total words: \" ++ show total)",
  "module Main where\n\nimport Data.Char (isAlphaNum, isDigit, isLower, isUpper)\nimport Data.List (intercalate)\n\nisValidUsername :: String -> Bool\nisValidUsername u =\n  length u >= 3\n    && length u <= 16\n    && all (\\c -> isAlphaNum c || c == '_') u\n    && not (isDigit (head u))\n\npasswordIssues :: String -> [String]\npasswordIssues p =\n  concat\n    [ [\"too short\" | length p < 8]\n    , [\"no uppercase\" | not (any isUpper p)]\n    , [\"no lowercase\" | not (any isLower p)]\n    , [\"no digit\" | not (any isDigit p)]\n    ]\n\nmain :: IO ()\nmain = do\n  let users = [\"alice_99\", \"ab\", \"9lives\", \"Bob_2024\"]\n  mapM_ (\\u -> putStrLn (u ++ \" -> \" ++ show (isValidUsername u))) users\n  let passes = [\"Hunter2\", \"weakpass\", \"Str0ngPass\"]\n  let report p =\n        case passwordIssues p of\n          [] -> putStrLn (p ++ \": accepted\")\n          xs -> putStrLn (p ++ \": rejected: \" ++ intercalate \", \" xs)\n  mapM_ report passes",
  "module Main where\n\nimport Data.List (sort)\n\nmean :: [Double] -> Double\nmean xs = sum xs / fromIntegral (length xs)\n\nmedian :: [Double] -> Double\nmedian xs\n  | odd n = sorted !! half\n  | otherwise = (sorted !! (half - 1) + sorted !! half) / 2\n  where\n    n = length xs\n    half = n `div` 2\n    sorted = sort xs\n\ndescribe :: String -> [Double] -> IO ()\ndescribe label xs = do\n  putStrLn (label ++ \":\")\n  putStrLn (\"  raw    = \" ++ show xs)\n  putStrLn (\"  min    = \" ++ show (minimum xs))\n  putStrLn (\"  max    = \" ++ show (maximum xs))\n  putStrLn (\"  mean   = \" ++ show (mean xs))\n  putStrLn (\"  median = \" ++ show (median xs))\n  putStrLn \"\"\n\nmain :: IO ()\nmain = do\n  describe \"week temps\" [21.5, 23.0, 19.8, 22.4, 25.1, 20.6, 24.3]\n  describe \"exam scores\" [88, 92, 79, 85, 94, 61, 73, 90]\n  describe \"single reading\" [42]",
  "module Main where\n\nimport Data.Char (isSpace)\nimport Data.List (foldl')\n\ncheck :: String -> (Int, Int)\ncheck = foldl' step (0, 0)\n  where\n    step (bal, low) ch\n      | ch == '(' = (bal + 1, low)\n      | ch == ')' = (bal - 1, min low (bal - 1))\n      | otherwise = (bal, low)\n\nwellFormed :: String -> Bool\nwellFormed raw =\n  let s = filter (not . isSpace) raw\n      (total, lowest) = check s\n  in total == 0 && lowest >= 0\n\nreport :: String -> IO ()\nreport s =\n  putStrLn\n    (s ++ \" -> \" ++ (if wellFormed s then \"balanced\" else \"broken\"))\n\nmain :: IO ()\nmain = do\n  report \"(a(b)c)d\"\n  report \"((x)\"\n  report \")(\"\n  report \"no brackets here\"\n  putStrLn \"\"\n  let batch = [\"(())\", \")(\", \"(a)\", \"b(c(d)e)f\", \"(()\"]\n  print (map wellFormed batch)",
];
