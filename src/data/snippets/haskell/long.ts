export const snippets = [
  "module Main where\n\nimport Text.Printf (printf)\n\n-- The famous lazy infinite list, defined in terms of itself.\nfibs :: [Integer]\nfibs = 0 : 1 : zipWith (+) fibs (tail fibs)\n\nmain :: IO ()\nmain = do\n  let taken = take 15 fibs\n  putStrLn \"First 15 Fibonacci numbers:\"\n  print taken\n  putStrLn \"\"\n  mapM_ showPair (zip [0 .. 14] taken)\n  let ratios =\n        zipWith (\\a b -> b / a)\n          (map fromIntegral taken)\n          (map fromIntegral (tail taken)) :: [Double]\n  putStrLn \"Ratios of neighbours creep toward phi:\"\n  mapM_ (printf \"%.6f\\n\") (take 10 ratios)\n  putStrLn \"\"\n  putStrLn \"Sum of even Fibonacci values not above 4000000:\"\n  let capped = takeWhile (<= 4000000) fibs\n  print (sum (filter even capped))\n  where\n    showPair :: (Int, Integer) -> IO ()\n    showPair (i, v) = do\n      printf \"index %2d -> %d\\n\" i v\n      pure ()",
  "module Main where\n\nquicksort :: Ord a => [a] -> [a]\nquicksort [] = []\nquicksort (p : xs) =\n  quicksort smaller ++ [p] ++ quicksort larger\n  where\n    smaller = filter (< p) xs\n    larger = filter (>= p) xs\n\nmain :: IO ()\nmain = do\n  let numbers = [33, 10, 68, 2, 45, 19, 87, 54, 7, 91]\n  putStrLn \"Before sorting:\"\n  print numbers\n  putStrLn \"\"\n  putStrLn \"After sorting:\"\n  print (quicksort numbers)\n  putStrLn \"\"\n  let fruits = [\"pear\", \"apple\", \"fig\", \"banana\", \"kiwi\", \"date\"]\n  putStrLn \"Fruits sorted:\"\n  print (quicksort fruits)\n  putStrLn \"\"\n  let uniqueSorted xs =\n        foldr (\\x acc ->\n          if x `elem` acc then acc else x : acc) [] xs\n  putStrLn \"Sorted without duplicates:\"\n  print (uniqueSorted numbers)\n  putStrLn \"Kept the same length?\"\n  print (length (uniqueSorted numbers) == length numbers)",
  "module Main where\n\nbinarySearch :: Ord a => [a] -> a -> Int\nbinarySearch xs target = go 0 (length xs - 1)\n  where\n    go lo hi\n      | lo > hi = -1\n      | otherwise =\n          let mid = (lo + hi) `div` 2\n              candidate = xs !! mid\n          in case compare candidate target of\n               EQ -> mid\n               LT -> go (mid + 1) hi\n               GT -> go lo (mid - 1)\n\nreport :: (Show a, Ord a) => [a] -> a -> IO ()\nreport hay t =\n  case binarySearch hay t of\n    -1 -> putStrLn (show t ++ \" -> absent\")\n    idx -> putStrLn (show t ++ \" -> index \" ++ show idx)\n\nmain :: IO ()\nmain = do\n  let hay = [2, 5, 8, 12, 16, 23, 38, 56, 72, 91]\n  putStrLn \"Sorted haystack:\"\n  print hay\n  putStrLn \"\"\n  let big = map (* 3) [1 .. 40]\n  mapM_ (report hay) [23, 91, 2, 50, 100]\n  putStrLn \"\"\n  report big 87",
  "module Main where\n\nisPrime :: Int -> Bool\nisPrime n\n  | n < 2 = False\n  | otherwise =\n      all (\\d -> n `mod` d /= 0)\n        (takeWhile (\\d -> d * d <= n) [2 ..])\n\nprimesUpTo :: Int -> [Int]\nprimesUpTo n = [p | p <- [2 .. n], isPrime p]\n\nadjacentPairs :: [Int] -> [(Int, Int)]\nadjacentPairs ps = zip ps (tail ps)\n\nmain :: IO ()\nmain = do\n  let limit = 60\n  putStrLn (\"Primes up to \" ++ show limit ++ \":\")\n  print (primesUpTo limit)\n  putStrLn \"\"\n  let twins =\n        filter (\\(a, b) -> b - a == 2)\n          (adjacentPairs (primesUpTo 100))\n  putStrLn \"Twin prime pairs below 100:\"\n  mapM_ print twins\n  putStrLn \"\"\n  let largest = last (primesUpTo limit)\n  putStrLn (\"Largest prime up to limit: \" ++ show largest)\n  putStrLn \"How many primes below 1000?\"\n  print (length (primesUpTo 999))",
  "module Main where\n\nimport Data.Char (isAlphaNum, toLower)\n\nnormalize :: String -> String\nnormalize = filter isAlphaNum . map toLower\n\nisPalindrome :: String -> Bool\nisPalindrome s =\n  let clean = normalize s\n  in clean == reverse clean\n\ndescribe :: String -> IO ()\ndescribe s = do\n  let verdict =\n        if isPalindrome s then \"a palindrome\" else \"not a palindrome\"\n  putStrLn (s ++ \" is \" ++ verdict)\n\nmain :: IO ()\nmain = do\n  describe \"A man a plan a canal Panama\"\n  describe \"racecar\"\n  describe \"Hello World\"\n  describe \"Was it a car or a cat I saw\"\n  describe \"12321\"\n  describe \"No lemon no melon\"\n  putStrLn \"\"\n  let samples = [\"abc\", \"abba\", \"ab ca\", \"xyzyx\"]\n  let results = map isPalindrome samples\n  print (zip samples results)",
];
