export const snippets = [
  "fn fib(n: u32) -> u64 {\n    let mut a: u64 = 0;\n    let mut b: u64 = 1;\n    for _ in 0..n {\n        let next = a + b;\n        a = b;\n        b = next;\n    }\n    a\n}\n\nfn main() {\n    for n in [5, 10, 20] {\n        println!(\"fib({}) = {}\", n, fib(n));\n    }\n}",
  "fn quicksort(items: &[i32]) -> Vec<i32> {\n    if items.len() <= 1 {\n        return items.to_vec();\n    }\n    let pivot = items[0];\n    let rest = &items[1..];\n    let left: Vec<i32> = rest.iter().filter(|&&x| x < pivot).copied().collect();\n    let right: Vec<i32> = rest.iter().filter(|&&x| x >= pivot).copied().collect();\n    let mut result = quicksort(&left);\n    result.push(pivot);\n    result.extend(quicksort(&right));\n    result\n}\n\nfn main() {\n    println!(\"{:?}\", quicksort(&[42, 7, 19, 3, 88, 23]));\n}",
  "fn binary_search(items: &[i32], target: i32) -> Option<usize> {\n    let mut lo = 0usize;\n    let mut hi = items.len();\n    while lo < hi {\n        let mid = (lo + hi) / 2;\n        match items[mid].cmp(&target) {\n            std::cmp::Ordering::Equal => return Some(mid),\n            std::cmp::Ordering::Less => lo = mid + 1,\n            std::cmp::Ordering::Greater => hi = mid,\n        }\n    }\n    None\n}\n\nfn main() {\n    let sorted = [1, 3, 5, 8, 13, 21, 34];\n    println!(\"{:?} {:?}\", binary_search(&sorted, 13), binary_search(&sorted, 4));\n}",
  "fn primes_up_to(limit: u32) -> Vec<u32> {\n    let mut sieve = vec![true; (limit + 1) as usize];\n    sieve[0] = false;\n    if limit >= 1 {\n        sieve[1] = false;\n    }\n    let mut result = Vec::new();\n    for i in 2..=limit {\n        if !sieve[i as usize] {\n            continue;\n        }\n        result.push(i);\n        let mut j = i * i;\n        while j <= limit {\n            sieve[j as usize] = false;\n            j += i;\n        }\n    }\n    result\n}\n\nfn main() {\n    println!(\"{:?}\", primes_up_to(30));\n}",
  "fn is_palindrome(text: &str) -> bool {\n    let clean: String = text\n        .to_lowercase()\n        .chars()\n        .filter(|c| c.is_alphanumeric())\n        .collect();\n    let chars: Vec<char> = clean.chars().collect();\n    let mut left = 0;\n    let mut right = chars.len();\n    while left < right {\n        right -= 1;\n        if chars[left] != chars[right] {\n            return false;\n        }\n        left += 1;\n    }\n    true\n}\n\nfn main() {\n    println!(\"{}\", is_palindrome(\"Never odd or even\"));\n}",
];
